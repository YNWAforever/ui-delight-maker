# Safe template for local development and preview environments.
# Copy to .env.local and replace placeholders; never commit real credentials.

APP_BASE_URL=http://localhost:5173
DATABASE_URL=postgresql://USER:PASSWORD@HOST/neondb?sslmode=require&channel_binding=require
NEON_AUTH_URL=https://YOUR_NEON_AUTH_URL

# Legacy Supabase project. Still REQUIRED until the Neon migration finishes: the authorization
# layer resolves ownership for deals, projects, contacts, customer-success profiles and
# automation playbooks/runs through src/legacy-supabase/server.ts, which throws when these are
# unset. Leaving them blank does not degrade those screens — it 500s them from inside the
# capability check. See "Migration In Progress" in CLAUDE.md.
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=replace-with-supabase-anon-key
# Server-to-server only; never expose to the client. Needed by scripts that must bypass RLS.
SUPABASE_SERVICE_ROLE_KEY=replace-with-supabase-service-role-key

# n8n app triggers. Invitation delivery stays disabled until explicitly approved.
N8N_QUALIFY_LEAD_WEBHOOK_URL=https://YOUR_N8N_HOST/webhook/clientops-qualify-lead
N8N_DRAFT_REPLY_WEBHOOK_URL=https://YOUR_N8N_HOST/webhook/clientops-draft-reply
N8N_DRAFT_QUOTE_WEBHOOK_URL=https://YOUR_N8N_HOST/webhook/clientops-draft-quote
N8N_SCORE_RENEWAL_RISK_WEBHOOK_URL=https://YOUR_N8N_HOST/webhook/clientops-score-renewal-risk
N8N_RELATIONSHIP_INTELLIGENCE_WEBHOOK_URL=https://YOUR_N8N_HOST/webhook/clientops-relationship-intelligence
N8N_USER_INVITATION_WEBHOOK_URL=
N8N_WORKFLOW_TOKEN=replace-with-a-long-random-token

# Optional direct workflow apply. Leave unset when importing JSON manually.
N8N_API_BASE_URL=https://YOUR_N8N_HOST
N8N_API_KEY=replace-with-n8n-public-api-key

# Optional live AI path. Workflows must still pass with deterministic fallback.
OPENROUTER_API_KEY=replace-with-openrouter-key
OPENROUTER_MODEL=anthropic/claude-sonnet-4-6

# Seed controls. Never point these at production.
CLIENTOPS_ALLOW_STAGING_SEED=0
CLIENTOPS_SEED_ON_DEPLOY=0
CLIENTOPS_SEED_MODE=staging-smoke
CLIENTOPS_SEED_TARGET=staging
CLIENTOPS_DESTRUCTIVE_RESET=
CLIENTOPS_SEED_TODAY=
CLIENTOPS_SMOKE_PROFILE_ID=neon-auth-user-id-for-staging-user
CLIENTOPS_SMOKE_PROFILE_EMAIL=clientops-smoke@example.com
CLIENTOPS_SMOKE_PROFILE_NAME=ClientOps Smoke User

# Required only for the one-time guarded bootstrap command.
CLIENTOPS_BOOTSTRAP_SUPER_ADMIN_EMAIL=