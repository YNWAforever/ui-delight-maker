{
  "name": "clientops-draft-quote",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "clientops-draft-quote",
        "responseMode": "responseNode"
      },
      "id": "clientops-draft-quote-webhook",
      "name": "clientops-draft-quote",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [0, 0]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first().json;\nconst headers = item.headers ?? {};\nconst body = item.body ?? item;\nconst receivedToken = headers['x-workflow-token'] ?? headers['X-Workflow-Token'];\nif (!receivedToken || receivedToken !== $env.N8N_WORKFLOW_TOKEN) {\n  throw new Error('Invalid x-workflow-token');\n}\nif (body.trigger !== 'quote.draft_requested') {\n  throw new Error(`Unexpected trigger: ${body.trigger ?? 'missing'}`);\n}\nif (!body.lead_id) {\n  throw new Error('Missing lead_id');\n}\nif (!body.agent_run_id) {\n  throw new Error('Missing agent_run_id');\n}\nreturn [{ json: { trigger: body.trigger, lead_id: body.lead_id, agent_run_id: body.agent_run_id } }];"
      },
      "id": "validate-draft-quote-trigger",
      "name": "Validate Trigger",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [220, 0]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{$env.APP_BASE_URL + '/api/workflows/context/lead'}}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "x-workflow-token",
              "value": "={{$env.N8N_WORKFLOW_TOKEN}}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ { lead_id: $json.lead_id, agent_run_id: $json.agent_run_id } }}",
        "options": {}
      },
      "id": "fetch-draft-quote-lead-context",
      "name": "Fetch Lead Context",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [440, 0]
    },
    {
      "parameters": {
        "jsCode": "const context = $input.first().json;\nconst triggerPayload = $('Validate Trigger').first().json;\nconst lead = context.lead ?? {};\nconst agentRunId = context.agent_run?.id ?? context.agent_run_id ?? triggerPayload.agent_run_id;\nconst pricing = Array.isArray(context.pricing_templates) ? context.pricing_templates : [];\nconst text = [lead.company_name, lead.contact_name, lead.contact_email, lead.enquiry_text].filter(Boolean).join(' ').toLowerCase();\nconst matches = pricing.filter((template) => [template.service, template.category, template.description].filter(Boolean).join(' ').toLowerCase().split(/\\s+/).filter((token) => token.length > 3).some((token) => text.includes(token))).slice(0, 2);\nconst selected = (matches.length > 0 ? matches : pricing).slice(0, matches.length > 0 ? 2 : 1);\nconst defaultCurrency = 'HKD';\nconst defaultPrice = 18000;\nconst addDays = (date, days) => {\n  const next = new Date(date);\n  next.setUTCDate(next.getUTCDate() + days);\n  return next.toISOString().slice(0, 10);\n};\nconst lineItems = selected.map((template, index) => ({\n  id: `line-${index + 1}`,\n  service: template.service,\n  description: template.description ?? `${template.service} support package`,\n  qty: 1,\n  unit_price: Number(template.unit_price ?? defaultPrice)\n}));\nconst finalLineItems = lineItems.length > 0 ? lineItems : [{ id: 'line-1', service: 'ClientOps Discovery Sprint', description: 'Discovery and recommendation sprint for the lead follow-up opportunity.', qty: 1, unit_price: defaultPrice }];\nconst fallback = {\n  lead_id: lead.id ?? triggerPayload.lead_id,\n  agent_run_id: agentRunId,\n  quote: {\n    number: `FIM-${String(agentRunId ?? 'run').slice(0, 8).toUpperCase()}`,\n    currency: selected[0]?.currency ?? defaultCurrency,\n    total_value: finalLineItems.reduce((sum, item) => sum + item.qty * item.unit_price, 0),\n    valid_until: addDays(new Date(), 14),\n    line_items: finalLineItems\n  },\n  create_send_approval: true,\n  context_summary: `Fallback quote drafted for ${lead.company_name ?? 'lead'}.`,\n  confidence_score: 0.6,\n  model_used: 'deterministic-fallback'\n};\nconst model = $env.OPENROUTER_MODEL || 'anthropic/claude-sonnet-4-6';\nconst openrouter_request = {\n  model,\n  messages: [\n    { role: 'system', content: 'Return only JSON matching the ClientOps draft quote writeback contract. Do not include markdown.' },\n    { role: 'user', content: JSON.stringify({ context, required_fields: ['lead_id', 'agent_run_id', 'quote', 'create_send_approval', 'context_summary', 'confidence_score'] }) }\n  ],\n  response_format: { type: 'json_object' }\n};\nreturn [{ json: { context, fallback, openrouter_request, openrouter_configured: Boolean($env.OPENROUTER_API_KEY) } }];"
      },
      "id": "prepare-draft-quote-fallback-ai-request",
      "name": "Prepare Fallback / AI Request",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [660, 0]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://openrouter.ai/api/v1/chat/completions",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "={{'Bearer ' + $env.OPENROUTER_API_KEY}}"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{$json.openrouter_request}}",
        "options": {}
      },
      "id": "openrouter-draft-quote-request",
      "name": "OpenRouter Request",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [880, 0],
      "continueOnFail": true
    },
    {
      "parameters": {
        "jsCode": "const prepared = $('Prepare Fallback / AI Request').first().json;\nconst fallback = prepared.fallback;\nconst response = $input.first()?.json ?? {};\nlet parsed;\nconst content = response.choices?.[0]?.message?.content;\nif (typeof content === 'string') {\n  try {\n    parsed = JSON.parse(content);\n  } catch (error) {\n    parsed = undefined;\n  }\n} else if (content && typeof content === 'object') {\n  parsed = content;\n}\nconst output = parsed && typeof parsed === 'object' ? { ...fallback, ...parsed } : fallback;\noutput.lead_id = output.lead_id || fallback.lead_id;\noutput.agent_run_id = output.agent_run_id || fallback.agent_run_id;\noutput.quote = output.quote || fallback.quote;\noutput.create_send_approval = typeof output.create_send_approval === 'boolean' ? output.create_send_approval : fallback.create_send_approval;\nreturn [{ json: output }];"
      },
      "id": "resolve-draft-quote-output",
      "name": "Resolve Output",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1100, 0]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{$env.APP_BASE_URL + '/api/workflows/draft-quote'}}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "x-workflow-token",
              "value": "={{$env.N8N_WORKFLOW_TOKEN}}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{$json}}",
        "options": {}
      },
      "id": "writeback-draft-quote",
      "name": "Writeback",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1320, 0]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{$json}}",
        "options": {}
      },
      "id": "respond-draft-quote-webhook",
      "name": "Respond To Webhook",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [1540, 0]
    }
  ],
  "connections": {
    "clientops-draft-quote": {
      "main": [
        [
          {
            "node": "Validate Trigger",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate Trigger": {
      "main": [
        [
          {
            "node": "Fetch Lead Context",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch Lead Context": {
      "main": [
        [
          {
            "node": "Prepare Fallback / AI Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Fallback / AI Request": {
      "main": [
        [
          {
            "node": "OpenRouter Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "OpenRouter Request": {
      "main": [
        [
          {
            "node": "Resolve Output",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Resolve Output": {
      "main": [
        [
          {
            "node": "Writeback",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Writeback": {
      "main": [
        [
          {
            "node": "Respond To Webhook",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  }
}
