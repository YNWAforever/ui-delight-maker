{
  "name": "clientops-qualify-lead",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "clientops-qualify-lead",
        "responseMode": "responseNode"
      },
      "id": "clientops-qualify-lead-webhook",
      "name": "clientops-qualify-lead",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [0, 0]
    },
    {
      "parameters": {
        "jsCode": "const item = $input.first().json;\nconst headers = item.headers ?? {};\nconst body = item.body ?? item;\nconst receivedToken = headers['x-workflow-token'] ?? headers['X-Workflow-Token'];\nif (!receivedToken || receivedToken !== $env.N8N_WORKFLOW_TOKEN) {\n  throw new Error('Invalid x-workflow-token');\n}\nif (body.trigger !== 'lead.qualify_requested') {\n  throw new Error(`Unexpected trigger: ${body.trigger ?? 'missing'}`);\n}\nif (!body.lead_id) {\n  throw new Error('Missing lead_id');\n}\nif (!body.agent_run_id) {\n  throw new Error('Missing agent_run_id');\n}\nreturn [{ json: { trigger: body.trigger, lead_id: body.lead_id, agent_run_id: body.agent_run_id } }];"
      },
      "id": "validate-qualify-trigger",
      "name": "Validate Trigger",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [220, 0]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{$env.APP_BASE_URL + '/api/workflows/context/lead'}}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "x-workflow-token",
              "value": "={{$env.N8N_WORKFLOW_TOKEN}}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ { lead_id: $json.lead_id, agent_run_id: $json.agent_run_id } }}",
        "options": {}
      },
      "id": "fetch-qualify-lead-context",
      "name": "Fetch Lead Context",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [440, 0]
    },
    {
      "parameters": {
        "jsCode": "const context = $input.first().json;\nconst triggerPayload = $('Validate Trigger').first().json;\nconst lead = context.lead ?? {};\nconst agentRunId = context.agent_run?.id ?? context.agent_run_id ?? triggerPayload.agent_run_id;\nconst pricing = Array.isArray(context.pricing_templates) ? context.pricing_templates : [];\nconst text = [lead.company_name, lead.contact_name, lead.contact_email, lead.enquiry_text].filter(Boolean).join(' ').toLowerCase();\nconst matches = pricing.filter((template) => [template.service, template.category, template.description].filter(Boolean).join(' ').toLowerCase().split(/\\s+/).filter((token) => token.length > 3).some((token) => text.includes(token))).slice(0, 2);\nconst selected = (matches.length > 0 ? matches : pricing).slice(0, 2);\nconst urgencyScore = /urgent|asap|launch|deadline|this month|q[1-4]/i.test(text) ? 8 : 6;\nconst fitScore = selected.length > 0 ? 8 : 6;\nconst score = Math.min(95, Math.round((urgencyScore + fitScore) * 5));\nconst fallback = {\n  lead_id: lead.id ?? triggerPayload.lead_id,\n  agent_run_id: agentRunId,\n  qualification_data: {\n    urgency_score: urgencyScore,\n    fit_score: fitScore,\n    qualification_score: score,\n    service_interest: selected.map((template) => template.service),\n    budget_range: /\\bhkd\\b|\\$|budget|retainer|monthly/i.test(text) ? 'HKD 50k-200k' : 'unknown',\n    next_action: score >= 75 ? 'Schedule discovery call' : 'Request more info',\n    reason: 'Deterministic staging fallback based on enquiry text and active pricing templates.',\n    confidence: 0.64,\n    human_review_required: true\n  },\n  lead_score: score,\n  output_summary: `Fallback qualification scored ${lead.company_name ?? 'lead'} at ${score}.`,\n  confidence_score: 0.64,\n  model_used: 'deterministic-fallback'\n};\nconst model = $env.OPENROUTER_MODEL || 'anthropic/claude-sonnet-4-6';\nconst openrouter_request = {\n  model,\n  messages: [\n    { role: 'system', content: 'Return only JSON matching the ClientOps qualification writeback contract. Do not include markdown.' },\n    { role: 'user', content: JSON.stringify({ context, required_fields: ['lead_id', 'agent_run_id', 'qualification_data', 'lead_score', 'output_summary', 'confidence_score', 'model_used'] }) }\n  ],\n  response_format: { type: 'json_object' }\n};\nreturn [{ json: { context, fallback, openrouter_request, openrouter_configured: Boolean($env.OPENROUTER_API_KEY) } }];"
      },
      "id": "prepare-qualify-fallback-ai-request",
      "name": "Prepare Fallback / AI Request",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [660, 0]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://openrouter.ai/api/v1/chat/completions",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "={{'Bearer ' + $env.OPENROUTER_API_KEY}}"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{$json.openrouter_request}}",
        "options": {}
      },
      "id": "openrouter-qualify-request",
      "name": "OpenRouter Request",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [880, 0],
      "continueOnFail": true
    },
    {
      "parameters": {
        "jsCode": "const prepared = $('Prepare Fallback / AI Request').first().json;\nconst fallback = prepared.fallback;\nconst response = $input.first()?.json ?? {};\n\nfunction parsePayload(value) {\n  const content = value.choices?.[0]?.message?.content;\n  if (typeof content === 'string') {\n    try {\n      return JSON.parse(content);\n    } catch (error) {\n      return undefined;\n    }\n  }\n  return isPlainObject(content) ? content : undefined;\n}\n\nfunction isPlainObject(value) {\n  return value !== null && typeof value === 'object' && !Array.isArray(value);\n}\n\nfunction finiteNumber(value, fallbackValue) {\n  const number = typeof value === 'number' ? value : Number(value);\n  return Number.isFinite(number) ? number : fallbackValue;\n}\n\nfunction clampNumber(value, fallbackValue, min, max) {\n  return Math.min(max, Math.max(min, finiteNumber(value, fallbackValue)));\n}\n\nfunction safeString(value, fallbackValue, maxLength = 1200) {\n  return typeof value === 'string' && value.trim() ? value.trim().slice(0, maxLength) : fallbackValue;\n}\n\nfunction safeJsonValue(value, depth = 0) {\n  if (depth > 4) return null;\n  if (value === null || typeof value === 'string' || typeof value === 'boolean') return value;\n  if (typeof value === 'number') return Number.isFinite(value) ? value : null;\n  if (Array.isArray(value)) return value.slice(0, 25).map((entry) => safeJsonValue(entry, depth + 1));\n  if (isPlainObject(value)) {\n    return Object.fromEntries(\n      Object.entries(value)\n        .slice(0, 50)\n        .map(([key, entry]) => [key.slice(0, 80), safeJsonValue(entry, depth + 1)]),\n    );\n  }\n  return null;\n}\n\nconst parsed = parsePayload(response);\nconst qualificationData = isPlainObject(parsed?.qualification_data)\n  ? safeJsonValue(parsed.qualification_data)\n  : fallback.qualification_data;\n\nconst output = {\n  lead_id: fallback.lead_id,\n  agent_run_id: fallback.agent_run_id,\n  qualification_data: qualificationData,\n  lead_score: clampNumber(parsed?.lead_score, fallback.lead_score, 0, 100),\n  output_summary: safeString(parsed?.output_summary, fallback.output_summary),\n  confidence_score: clampNumber(parsed?.confidence_score, fallback.confidence_score, 0, 1),\n  model_used: safeString(parsed?.model_used, fallback.model_used, 120),\n};\nreturn [{ json: output }];"
      },
      "id": "resolve-qualify-output",
      "name": "Resolve Output",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1100, 0]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{$env.APP_BASE_URL + '/api/workflows/qualify-lead'}}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "x-workflow-token",
              "value": "={{$env.N8N_WORKFLOW_TOKEN}}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{$json}}",
        "options": {}
      },
      "id": "writeback-qualify-lead",
      "name": "Writeback",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1320, 0]
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{$json}}",
        "options": {}
      },
      "id": "respond-qualify-webhook",
      "name": "Respond To Webhook",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [1540, 0]
    }
  ],
  "connections": {
    "clientops-qualify-lead": {
      "main": [
        [
          {
            "node": "Validate Trigger",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate Trigger": {
      "main": [
        [
          {
            "node": "Fetch Lead Context",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch Lead Context": {
      "main": [
        [
          {
            "node": "Prepare Fallback / AI Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Prepare Fallback / AI Request": {
      "main": [
        [
          {
            "node": "OpenRouter Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "OpenRouter Request": {
      "main": [
        [
          {
            "node": "Resolve Output",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Resolve Output": {
      "main": [
        [
          {
            "node": "Writeback",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Writeback": {
      "main": [
        [
          {
            "node": "Respond To Webhook",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  }
}
