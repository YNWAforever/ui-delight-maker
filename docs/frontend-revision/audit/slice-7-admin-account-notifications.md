## PARITY

| Route | Purpose (one clause) | Loader / routeQueryOptions | Query keys used | Server functions (read) | Mutations -> server function | Capabilities | Nav entry (yes/no + group) | Before: what actually works today | Integrity finding IDs |
|---|---|---|---|---|---|---|---|---|---|
| `/admin` (`src/routes/admin.tsx`) | Admin subtree layout + role gate | **No loader.** `beforeLoad` (admin.tsx:7-16) calls `getAdminNavigationFn()` **directly — outside the query cache**; returns `{ navigation }` into route context | none | `getAdminNavigationFn` (`src/server-functions/admin-users.ts:119-137`) | none | Gate: `AdminError` `FORBIDDEN`/`OUTSIDE_SCOPE` → `redirect({to:"/"})` (admin.tsx:11-13); server side `requireAnyCapability(["users.view","teams.view","permissions.view","audit.view"])` then per-item `requireCapability` | yes — Administration group, one "Admin workspace" entry to `adminNavigation[0].href` (`src/components/app-sidebar.tsx:149-165`) | Renders `AdminShell` + `<Outlet/>` (admin.tsx:32-34); the inner nav is capability-filtered server-side and highlights correctly (`admin-shell.tsx:47-50`) | IF-E2-17 |
| `/admin/` (`src/routes/admin.index.tsx`) | Org-health overview + recent security events | `loader: ensureQueryData(adminOverviewQueryOptions())` (admin.index.tsx:24); `routeQueryOptions` at 11-21 | `crmQueryKeys.admin.section("overview","summary")` (admin.index.tsx:9) | `getAdminOverviewFn` (`admin-users.ts:139-142`), `getAdminAuditSummaryFn` (`admin-access.ts:182-194`, swallows FORBIDDEN → `[]`) | none | none in file; inherited from `/admin` | no own entry — is the `/admin` sidebar target and AdminShell "Overview" | 6 live metric tiles + 5 real audit rows render from the server; every tile is a link | IF-E2-20, IF-E2-21, IF-E2-22 |
| `/admin/people` (`src/routes/admin.people.tsx`) | People directory + invite / role / lifecycle management | `loaderDeps:{search}`; async loader, two parallel `ensureQueryData` (56-91), catches FORBIDDEN → `{forbidden:true}` | `crmQueryKeys.admin.list({resource:"people",...toUserFilters(search)})` (43, 59); `crmQueryKeys.admin.detail(profileId)` (49, 67) | `getAdminUsersFn`, `getAdminUserFn`, `getAdminReassignmentInventoryFn` (all `admin-users.ts`) | `suspendAdminUserFn` (192), `deactivateAdminUserWithReassignmentFn` (197), `inviteUsers` (252), `changeAdminUserRoleFn` (268) — all bare `await` + `refreshPeople()` (126-148) | `canInvite = ["super_admin","admin","manager"]` (123); `canManageLifecycle = ["super_admin","admin"]` (124); **no gate on role change** (238) | no sidebar entry; AdminShell "People" | Search/status/role filters, pagination, row select, invite, role change and suspend/deactivate-with-reassignment all reach real server functions | IF-E2-01, 02, 05, 06, 07, 08, 09, 10, 11, 12, 13, 14, 15, 16, 17 |
| `/admin/people/$id` (`src/routes/admin.people.$id.tsx`) | Full read-only user record, 6 tabs | async loader `ensureQueryData(adminUserQuery(params.id))` (19-31), catches FORBIDDEN | `crmQueryKeys.admin.detail(profileId)` (14) | `getAdminUserFn` only | **none** | forbidden panel only (49-58); no `profile.role` checks | no | **Nothing — the route never renders.** It nests under `AdminPeopleRoute` (`routeTree.gen.ts:290-293`) and `admin.people.tsx` has no `<Outlet/>`; both loaders run, the People directory is what you see | IF-E2-01, 02, 18, 19 |
| `/admin/teams` (`src/routes/admin.teams.tsx`) | Department/team directory + unit editing + membership | `loaderDeps:{search}`; `Promise.all` of three `ensureQueryData` (78-117) | `crmQueryKeys.admin.section("organization","directory")` (36); `admin.section("${kind}:${id}","organization-unit")` (37-38); `admin.section("people","team-member-options")` / `admin.detail(id)` (39-42) | `getAdminOrganizationFn`, `getAdminOrganizationUnitFn`, `getAdminUsersFn` | `createDepartmentFn`/`updateDepartmentFn`/`createTeamFn`/`updateTeamFn` (193-199), `upsertAdminTeamMembershipFn` (222, 238), `endAdminTeamMembershipFn` (252) — all bare `await` + `refreshOrganization()` (170-186) | `canManageDepartment=["super_admin","admin"]` (159); `canManageTeam=["super_admin","admin","manager"]` (160); handlers passed conditionally (295-297) | no sidebar entry; AdminShell "Teams" | Kind tabs, status filter, unit select, create/edit unit, add/role-change/end membership all reach real server functions | IF-E2-03, 23, 24, 25, 26, 27, 28, 29 |
| `/admin/teams/$id` (`src/routes/admin.teams.$id.tsx`) | Single org-unit detail, edit-only | `loaderDeps:{search}`; `Promise.all` of two `ensureQueryData` (67-73). **No `AdminError` catch** | same three key helpers (31-37), re-declared verbatim | `getAdminOrganizationUnitFn`, `getAdminUsersFn` | `updateDepartmentFn`/`updateTeamFn` (128-132), `upsertAdminTeamMembershipFn` (155,171), `endAdminTeamMembershipFn` (184) | `canManage` from `detail.kind` (102-105) | no | **Nothing — the route never renders.** Nests under `AdminTeamsRoute` (`routeTree.gen.ts:285-288`) and `admin.teams.tsx` has no `<Outlet/>`. It also has **zero inbound links** anywhere in `src` | IF-E2-03, 04, 26, 29, 30 |
| `/admin/access` (`src/routes/admin.access.tsx`) | Access-request queue + effective-permission review | `loaderDeps:{search}`; `Promise.all` + follow-up `ensureQueryData` (65-87), catches FORBIDDEN | `admin.list({scope:"access-requests",status})` (33-34); `admin.list({scope:"access-users",...})` (35-40); `admin.section(profileId,"access-overrides",{includeHistory:true})` (41-42); plus `admin.section("overview","summary")`, `admin.section("organization","directory")`, `admin.section("team:id","organization-unit")` for invalidation | `getAdminAccessRequestsFn`, `getAdminUsersFn`, `getAdminOverridesFn` | `decideAdminAccessRequestFn` (200), `createAdminPermissionOverrideFn` (205) — bare `await` + `refreshAdminAccessCaches()` (170-197, cache-scanning) | forbidden panel (218-227); `actorRole` to queue (285); `profile?.role === "super_admin"` gates Create override (299) and `canCreateOverride` (328); reads `ROLE_GRANTS`/`CAPABILITIES` for the table (310-313) | no sidebar entry; AdminShell "Access" | Profile select, requests/effective tabs, approve/reject with reason + temporary expiry, and create-override all reach real server functions | IF-E2-31, 32, 33, 34, 35 |
| `/admin/audit` (`src/routes/admin.audit.tsx`) | Immutable admin audit search + export | `loaderDeps:{search}`; `ensureQueryData(auditQueryOptions(search))` (36-37); queryFn wraps FORBIDDEN → `{forbidden:true}` (18-30) | `crmQueryKeys.admin.list({scope:"audit",...auditFilters(search)})` (12-13) | `getAdminAuditLogsFn` (`admin-access.ts:174-180`) | no writes. One side-effecting read: `exportAdminAuditLogsFn` (85) — no invalidation (correct) | forbidden panel (101-110); server side `audit.view` / `audit.export` | no sidebar entry; AdminShell "Audit" | Actor/target-type/target/action/severity filters, pagination, per-row before/after snapshot disclosure, and a working JSON download all reach real server functions | IF-E2-36, 37, 38, 39, 40 |
| `/account` (`src/routes/account.tsx`) | Personal profile, security, workload, availability, access requests | `loader: ensureQueryData(accountQueryOptions())` (29); `routeQueryOptions` 19-23 | `crmQueryKeys.account.detail("me")` (17) | `getMyAccount` (`src/server-functions/account.ts:93-133`) | `updateMyProfile` (72), `updateMyAvailability` (75), `revokeMyAppSessions` (78), `createMyDelegation` (81), `cancelMyDelegation` (84), `createMyAccessRequest` (87) — all via `runMutation` (41-65) which invalidates `accountQueryKey` exact + optionally `crmQueryKeys.shell()` | none — `requireNeonAuthSession()` only, every write is self-scoped | **no** — zero inbound links in `src`; only reachable via `redirect({href:"/account?welcome=1"})` (`invite.$token.complete.tsx:16`) | All six writes reach real server functions and the page re-reads correctly; profile edit also refreshes the shell | IF-E2-42, 43, 44, 45, 46, 47, 48, 49, 50 |
| `/notifications` (`src/routes/notifications.tsx`) | Notification inbox with type filters | `loader: ensureQueryData(routeQueryOptions({queryKey: crmQueryKeys.notifications.list({}), queryFn: getNotifications}))` (41-47); component uses `useNotifications()` | `crmQueryKeys.notifications.list({})` (44; `use-notifications.ts:18`) | `getNotifications` (`src/server-functions/notifications.ts:11-18`) | `markNotificationReadFn` (`use-notifications.ts:98`), `markAllNotificationsReadFn` (112) — optimistic with token-guarded rollback, then `invalidateQueries({exact:true})` | none — `requireNeonAuthSession()` only | **no** — only inbound is `NotificationBell` (`src/components/notification-bell.tsx:148`) | Mark-read and mark-all-read reach real server functions with correct optimistic rollback and correct exact invalidation; filter tabs filter the already-loaded list | IF-E2-51, 52 |

## INTEGRITY

| ID | Route | Element (file:line) | Category | Verdict | Owner | Fix step | Note |
|---|---|---|---|---|---|---|---|
| IF-E2-01 | /admin/people, /admin/people/$id | `src/routes/admin.people.tsx:222-286` (component returns a fragment; `Outlet` is never imported or rendered) vs `src/routeTree.gen.ts:290-293` `AdminPeopleIdRoute.getParentRoute = () => AdminPeopleRoute` | unscoped or capability-inappropriate navigation | UNAVAILABLE | frontend | Add the repo's standard layout idiom to `admin.people.tsx` — `if (!useIsExactPath("/admin/people")) return <Outlet/>` — matching `accounts.tsx:48`, `leads.tsx:93`, `quotes.tsx:80` | `/admin/people/$id` is registered, its loader runs, its component never mounts. Navigating there re-renders the People directory with no `?user=`, so the right panel reads "Select a person to review their record." Every other nested parent in the repo renders an Outlet; these two admin parents are the only ones that do not. No test covers it (`src/routes/__tests__/-admin-route-access.test.tsx:13` stubs `Outlet: () => null`) |
| IF-E2-02 | /admin/people | "Open full record" `src/components/admin/user-detail-panel.tsx:99-105`, href built at `src/routes/admin.people.tsx:237` | unscoped or capability-inappropriate navigation | UNAVAILABLE | frontend | Once IF-E2-01 lands, convert to `<Link to="/admin/people/$id" params={{id}}/>` | Raw `<a href>` forces a full document reload, and the destination is the dead route from IF-E2-01, so the user pays a full reload to land back on the same directory |
| IF-E2-03 | /admin/teams, /admin/teams/$id | `src/routes/admin.teams.tsx:275-315` (no `<Outlet/>`) vs `src/routeTree.gen.ts:285-288` | unscoped or capability-inappropriate navigation | UNAVAILABLE | frontend | Same Outlet fix; then add an inbound link from `organization-unit-detail.tsx` | Worse than IF-E2-01: `grep -rn "admin/teams/"` across `src` finds **no link at all** to `/admin/teams/$id`. It is reachable only by typing the URL, and even then renders the parent. Its own "Back to organization" link (`admin.teams.$id.tsx:205-217`) is therefore also dead |
| IF-E2-04 | /admin/teams/$id | Loader `src/routes/admin.teams.$id.tsx:67-73`; component key `src/routes/admin.teams.$id.tsx:85` `detailQuery(search.kind, loaded.detail?.unit.id ?? "missing")` | raw database or driver error shown to users | REAL | frontend | Wrap the loader in the `AdminError` FORBIDDEN/OUTSIDE_SCOPE/CONFLICT catch used by `admin.teams.tsx:61-73`, and key the query off `params.id` | Unique in the slice: no `AdminError` catch, so FORBIDDEN escapes to `__root.tsx:53-81`, which renders `{error.message}` verbatim. Also `kind` defaults to `"department"` (`schemas.ts:162`), so a team id visited without `?kind=team` calls `getAdminOrganizationUnitFn({kind:"department", id:teamId})`; and the component key diverges from the loader key, caching under `"department:missing"` |
| IF-E2-05 | /admin/people | `src/components/admin/invite-users-dialog.tsx:78-87` — `"status" in entry.delivery && entry.delivery.status === "missing_webhook"` | success toast without a server action | REAL | frontend | Read `entry.delivery.delivered === false && entry.delivery.reason === "missing_webhook"` | **`dispatchInvitationEmail` returns `{ delivered: false, reason: "missing_webhook" }` (`src/server/admin/invitation-email.server.ts:19-28`) — there is no `status` key.** The branch is dead, so `missingWebhook` is always 0 and the dialog always shows "Invitations sent successfully." even when `N8N_USER_INVITATION_WEBHOOK_URL` is unset and no email was ever dispatched. This is a **third** instance of the sentinel-ignored class beyond the two the repo-map names |
| IF-E2-06 | /admin/people | `src/routes/admin.people.tsx:253` `toast.success("Invitation batch processed")` | success toast without a server action | REAL | frontend | Toast from the returned `delivery` result, or drop the route toast and keep only the dialog's `role="status"` | Fires unconditionally after `inviteUsers` resolves. Combined with IF-E2-05 the user gets two independent success signals for an invitation whose email may never have been sent |
| IF-E2-07 | /admin/people | `src/components/admin/invite-users-dialog.tsx:64-72` — `invitationInputSchema.parse(...)` is **outside** the `try` at 75 | raw database or driver error shown to users | REAL | frontend | Move the `.parse` inside the `try`, or validate with `safeParse` and surface the field error | The pre-check at 56-59 only tests `includes("@")` and `!endsWith("@")`, so `"@fimmick.com"` passes it and then `z.email()` throws. The `ZodError` escapes `submit`, `setError` never runs, `setSubmitting` never runs — the user sees nothing at all and the button stays live |
| IF-E2-08 | /admin/people | `src/routes/admin.people.tsx:248-257` passes no `departments` / `managers` / `teams`; fallbacks at `invite-users-dialog.tsx:183-189` and `211-217`; teams fieldset at `221-246` | disabled action without explanation | UNAVAILABLE | frontend | Feed the dialog from `getAdminOrganizationFn()` and `getAdminUsersFn()` (both already loaded on `/admin/teams`) | Department and manager degrade to free-text inputs asking for a raw UUID ("Department ID (optional)", "Manager profile ID (optional)"). The **Initial teams** fieldset never renders at all, so `initialTeamIds` is always `[]` — and that is exactly the field `requireInvitationTargets` (`admin-invitations.ts:72-76`) uses to scope a manager's invite authority |
| IF-E2-09 | /admin/people | "Filter by activity" select `src/components/admin/people-directory.tsx:153-165` | local-only switch representing persisted state | READ-ONLY | backend | Add `activity` to `AdminUserFilters` + `buildUserWhere`, or remove the control and the schema field | `toUserFilters` (`admin.people.tsx:29-39`) never forwards `activity`, and `AdminUserFilters` (`src/server/repositories/admin-users.ts:18-26`) has no such field. Changing it rewrites the URL but produces an **identical query key**, so React Query serves the same cached rows: a filter that visibly does nothing. Same dead-schema problem for `sort` (`schemas.ts:21`; SQL is a fixed `order by lower(coalesce(p.name,p.email,p.id))` at `admin-users.ts:256`) and `manager` (`schemas.ts:18`). `department`/`team` *are* forwarded but have no rendered control — and the `departments`/`teams`/`managers` props declared at `people-directory.tsx:19-21` are never destructured at 41-51 |
| IF-E2-10 | /admin/people | Row overflow button `src/components/admin/people-directory.tsx:247-255` — `onClick={(event) => event.stopPropagation()}` | disabled action without explanation | REMOVED | frontend | Delete it, or wire a real `DropdownMenu` with Change role / Manage lifecycle honouring `canInvite`/`canManageLifecycle` | Not disabled — inert. It renders a `MoreHorizontal` icon, `title="Open actions"` and `aria-label={"Open actions for " + name}`, and its only behaviour is suppressing the row's own select. Screen-reader users are told an actions menu exists |
| IF-E2-11 | /admin/people | "Change role" `src/components/admin/user-detail-panel.tsx:81-87`, wired unconditionally at `src/routes/admin.people.tsx:238` | disabled action without explanation | REAL | frontend | Gate with a `canManageRole` flag alongside `canInvite` (123) and `canManageLifecycle` (124) | `read_only` holds `users.view` and `teams.view` (`policy.ts:136`), so `requireAnyCapability` (`admin-users.ts:120`) lets it into `/admin` and `getAdminUsersFn` lets it list people. "Change role" is then offered with no gate, while Invite and Manage lifecycle beside it *are* gated. `changeAdminUserRoleFn` correctly refuses (`users.manage`), so the failure is contained — but only after the user fills in a reason and submits |
| IF-E2-12 | /admin/people | `src/components/admin/user-role-dialog.tsx:21-26` — `useState` initialised once, `if (!open) return null` at 26 keeps the component mounted | local-only switch representing persisted state | REAL | frontend | Add the `useEffect(() => { if (open) reset() }, [open, currentRole, userName])` reset that `user-lifecycle-dialog.tsx:54-61`, `organization-unit-dialog.tsx:52-72` and `permission-override-dialog.tsx:37-45` all already have | The component is hidden, not unmounted, so `role` and `reason` survive across openings. Open for user A, pick "Admin", cancel, open for user B: the select still reads "Admin" while the header says B's real current role, and B's previously typed reason is pre-filled and will be written to the audit log. The three sibling dialogs get this right; this one does not |
| IF-E2-13 | /admin/people | `src/components/admin/user-lifecycle-dialog.tsx:141-172` offers only Suspend / Deactivate | disabled action without explanation | UNAVAILABLE | frontend | Add a reactivate branch calling `reactivateAdminUserFn` when `user.status === "suspended"` | `reactivateAdminUserFn` exists and is authorized (`admin-users.ts:208-210`, `users.manage`), and `lifecycleActionSchema` (`schemas.ts:54-58`) already names `"reactivate"`. No route imports it. A suspended user cannot be restored from any screen in the product |
| IF-E2-14 | /admin/people | `src/components/admin/user-lifecycle-dialog.tsx:196-198` "Load the current ownership inventory before deactivation."; driven by `src/routes/admin.people.tsx:279` `inventory={lifecycleLoading ? undefined : lifecycleInventory}` | disabled action without explanation | READ-ONLY | frontend | Pass `lifecycleLoading` through and render a real loading state; keep the current copy only for the genuine load-failure case | `lifecycleLoading` and "inventory failed to load" collapse into the same undefined, so during a normal fetch the dialog tells the admin to perform an action they cannot perform while the submit button sits disabled (`canSubmit`, 70) with no reason given |
| IF-E2-15 | /admin/people | `refreshPeople` `src/routes/admin.people.tsx:126-148` | stale invalidation | REAL | frontend | Add `crmQueryKeys.admin.section("people","team-member-options")` and `crmQueryKeys.admin.section("organization","directory")` to the refresh set | Invalidates `admin.lists()`, `admin.section("overview","summary")`, optionally `admin.detail(id)` and `shell()`. It misses the team-member-options key that `/admin/teams` and `/admin/teams/$id` read (`admin.teams.tsx:39-42`), so after a suspend or deactivate the org screens keep offering the removed person as an active member/successor from cache |
| IF-E2-16 | /admin/people | `src/routes/admin.people.tsx:170-174` `toast.error(error.message)` | raw database or driver error shown to users | REAL | frontend | Route through the `toSafeErrorMessage` helper repo-map §Libraries schedules for B5 | `getAdminReassignmentInventoryFn` reaches `getReassignmentInventory` and `requireCapability` → `loadAuthorizationContext` runs raw SQL (`authorization.server.ts:38-80`). A driver failure surfaces its verbatim message in a toast. No sanitizer exists at any layer today |
| IF-E2-17 | /admin (all sub-routes) | `src/routes/admin.tsx:7-16` `beforeLoad` fetches navigation **outside the query cache**; `src/routes/admin.people.tsx:271` `refreshPeople(roleUser.id, true)` invalidates `crmQueryKeys.shell()` only | stale invalidation | REAL | frontend | After any capability-affecting write, also `router.invalidate({ filter: (m) => m.routeId === "/admin" })` — the VF-3 rule applied to a `beforeLoad` | Same class as VF-3 but for `beforeLoad` rather than a loader. `crmQueryKeys.shell()` drives the *outer* sidebar's `adminNavigation` (`__root.tsx:164`), so that refreshes; the *inner* `AdminShell` nav comes from route context and does not. Change a role that removes `audit.view` and the Audit tab stays in the admin sidebar until a hard reload — clicking it then lands on the forbidden panel |
| IF-E2-18 | /admin/people/$id | Security tab `src/routes/admin.people.$id.tsx:158-176` | disabled action without explanation | UNAVAILABLE | frontend | Add a revoke-sessions action calling `revokeAdminUserSessionsFn`, gated on `sessions.revoke` | The tab displays `sessionInvalidBefore` as a read-only fact. `revokeAdminUserSessionsFn` exists and is authorized (`admin-users.ts:228-235`, capability `sessions.revoke`) but is imported by no route. `/account` has the self-service equivalent (`account-settings.tsx:394-400`); the admin-side one does not exist |
| IF-E2-19 | /admin/people/$id | Access tab `src/routes/admin.people.$id.tsx:107-118` | "coming soon" as active navigation | READ-ONLY | frontend | Load `getAdminOverridesFn({profileId})` and render `EffectiveAccessTable`, exactly as `/admin/access` already does at `admin.access.tsx:309-315` | A tab labelled "Access" that renders one sentence of prose plus `Base role: {user.role}`. No grants, no overrides, no data fetch — while the identical view already exists and works on `/admin/access` |
| IF-E2-20 | /admin/ | Metric hrefs `src/components/admin/admin-overview.tsx:30-41` | unscoped or capability-inappropriate navigation | READ-ONLY | frontend | Use `<Link to search={...}>` with keys the destination schemas actually declare | Three of six carry parameters the target `validateSearch` silently drops: `/admin/teams?filter=managerless` and `/admin/access?filter=expiring` (`adminOrganizationSearchSchema` `schemas.ts:161-167` and `adminAccessSearchSchema` `171-178` have no `filter`), and `/admin/access?status=pending` (the key is `requestStatus`, not `status`). "Managerless teams: 3" therefore lands on the full, unfiltered directory. The three `/admin/people?status=…` links are correct |
| IF-E2-21 | /admin/ | Audit-event links `src/components/admin/admin-overview.tsx:145` `"/admin/audit?targetId=" + encodeURIComponent(...)` | unscoped or capability-inappropriate navigation | READ-ONLY | frontend | Emit `?target=` | `adminAuditSearchSchema` (`schemas.ts:182-191`) declares `target`, not `targetId`. Clicking a recent security event opens the audit page unfiltered — the one navigation on the overview that most needs to carry its filter |
| IF-E2-22 | /admin/ | `src/components/admin/admin-overview.tsx:55-72`, `92-98`, `133-138`, `143-160` | unscoped or capability-inappropriate navigation | READ-ONLY | frontend | Replace all four with `<Link>` | Every navigation on the admin landing page is a raw `<a href>`, so each one is a full document reload that re-runs the root `beforeLoad` shell fetch and the `/admin` `beforeLoad` navigation fetch. `admin-shell.tsx:52-65` uses `<Link>` correctly, so the two disagree within one screen |
| IF-E2-23 | /admin/teams | "Create organization unit" `src/components/admin/organization-directory.tsx:55-62`; handler `src/routes/admin.teams.tsx:263-273` | disabled action without explanation | REAL | frontend | Pass `canManageDepartment`/`canManageTeam` into `OrganizationDirectory` and gate or disable-with-reason, as `PeopleDirectory` does with `canInvite` (`people-directory.tsx:96`) | Rendered unconditionally. A `read_only` or `manager` actor sees the button, clicks it, and only then gets `toast.error("Department management is outside your access scope.")`. `canManageDepartment` and `canManageTeam` are already computed two lines away at 159-160 |
| IF-E2-24 | /admin/teams | Search input `src/components/admin/organization-directory.tsx:65-77` | unscoped or capability-inappropriate navigation | REAL | frontend | Hold a local draft and debounce, mirroring `people-directory.tsx:52-75` | `onChange` applies `.trim()` and immediately `navigate({replace:true})`. Because `value={search.q ?? ""}` reads back the trimmed URL value, a space is erased as you type it — "sales ops" is unreachable. It also writes a history entry per keystroke. The sibling directory debounces at 250 ms |
| IF-E2-25 | /admin/teams, /admin/teams/$id | Tabs `src/components/admin/organization-unit-detail.tsx:87-103`; bodies at `195-203` (permissions) and `205-213` (activity) | "coming soon" as active navigation | READ-ONLY | frontend | Load scoped overrides for the unit and unit-scoped audit rows, or drop the two tabs from `ADMIN_ORGANIZATION_TABS` (`schemas.ts:153-159`) | Two of five tabs are active, URL-persisted destinations whose entire content is a sentence of prose with no data source. "Activity" restates `unit.createdAt` and points at an audit log it does not link to |
| IF-E2-26 | /admin/teams, /admin/teams/$id | `addMembers` `src/routes/admin.teams.tsx:218-235`, `updateMember` `237-249`, `endMember` `251-261`; identical at `src/routes/admin.teams.$id.tsx:151-192` | success toast without a server action | REAL | frontend | Wrap each in try/catch with `toast.error`, and give `TeamMemberTable` a pending flag | None of the six handlers has a `catch`. `TeamMemberTable` calls them fire-and-forget (`team-member-table.tsx:61`, `166`, `188`), so a rejected `upsertAdminTeamMembershipFn` becomes an unhandled promise rejection: no toast, no `role="alert"`, and — because there is no optimistic write — the row simply does not change. The user cannot distinguish "saved" from "refused". `saveUnit` is the exception: it propagates into `OrganizationUnitDialog`'s catch (`organization-unit-dialog.tsx:108-113`) |
| IF-E2-27 | /admin/teams, /admin/teams/$id | `src/components/admin/team-member-table.tsx:52-66` — `setSelectedIds([])`, `setStartsAt("")`, `setEndsAt("")` run synchronously after a non-awaited `onAddMembers` | success toast without a server action | REAL | frontend | Await the handler, clear only on success, and disable the button while it runs | The form clears the instant the button is pressed, which reads as confirmation. Combined with IF-E2-26 a rejected add wipes the selection with no message and no way to recover what was chosen |
| IF-E2-28 | /admin/teams, /admin/teams/$id | Role select `src/components/admin/team-member-table.tsx:162-173`; "End membership" `186-192`; "Add selected members" `124-130` | replay/retry without idempotency | REAL | frontend | Track a per-row pending id (the pattern `access-request-queue.tsx:33` already uses) and disable while in flight | No in-progress state anywhere in this table. `endAdminTeamMembershipFn` stamps a fresh `new Date().toISOString()` per call (`admin.teams.tsx:256`), so a double-click writes two different `endedAt` values and two audit rows for one action |
| IF-E2-29 | /admin/teams, /admin/teams/$id | `refreshOrganization` `src/routes/admin.teams.tsx:170-186`; identical `src/routes/admin.teams.$id.tsx:107-123` | stale invalidation | REAL | frontend | Add `crmQueryKeys.admin.lists()` (prefix, not exact) to the key set | Invalidates the org directory, the unit, the member-options list and per-profile details — but never the people-directory list key `admin.list({resource:"people",...})`. `listAdminUsers` selects a live `team_count` sub-query (`admin-users.ts:238-244`), so after adding or ending a membership `/admin/people` keeps serving stale team counts from cache. Exactly the mirror image of IF-E2-15 |
| IF-E2-30 | /admin/teams/$id | `updateMember` `src/routes/admin.teams.$id.tsx:170-181`, `endMember` `183-192` | success toast without a server action | REAL | frontend | Add the `toast.success` calls the sibling route has | The `/admin/teams` copies toast "Membership role updated" (`admin.teams.tsx:247`) and "Membership ended" (259); the `$id` copies are byte-identical except that both toasts were dropped. Two screens, same action, different feedback contract |
| IF-E2-31 | /admin/access | `OverrideHistory` `src/routes/admin.access.tsx:91-137`; `EffectiveAccessTable` `src/components/admin/effective-access-table.tsx:53-84` | disabled action without explanation | UNAVAILABLE | frontend | Add a revoke control per active override calling `revokeAdminPermissionOverrideFn`, gated on `profile?.role === "super_admin"` | `revokeAdminPermissionOverrideFn` exists and is authorized (`admin-access.ts:90-96`, `permissions.override`) but is not imported by any route. The history table renders a "Revoked" state (`admin.access.tsx:128`) that nothing in the product can produce. A permanent (non-expiring) override created here can only be undone with direct SQL |
| IF-E2-32 | /admin/access | `src/components/admin/permission-override-dialog.tsx:123-183` — capability, effect, reason, expiry only | disabled action without explanation | UNAVAILABLE | frontend | Add optional department / team / resource-type / resource-id fields bound to the existing schema fields | `permissionOverrideSchema` (`schemas.ts:76-86`) accepts `departmentId`, `teamId`, `resourceType`, `resourceId`, and `overrideIsActive` (`policy.ts:150-153`) enforces all four. The dialog never sends them and `createOverride` (`admin.access.tsx:204-216`) never forwards them, so **every override the product can create is org-wide and unscoped** — the broadest grant the policy engine supports, with no way to say so or narrow it. Security-relevant |
| IF-E2-33 | /admin/access | `src/components/admin/access-request-queue.tsx:125-127` — literal `Pending` span | local-only switch representing persisted state | READ-ONLY | frontend | Render `request.status` (via the `status-labels.ts` map repo-map §Libraries schedules for B7) and feed `StatusBadge` | A hardcoded amber "Pending" pill on every row regardless of the record's real status. `getAdminAccessRequestsFn` accepts `approved`/`rejected`/`cancelled`/`all` (`admin-access.ts:41-43`), so any of those lists renders every row as Pending |
| IF-E2-34 | /admin/access | `src/components/admin/access-request-queue.tsx:131-138` — disabled "Capability approval unavailable" | disabled action without explanation | READ-ONLY | frontend | Replace the label with the reason and keep an enabled reject path | The button says what is unavailable, never why. The rule is `decideAdminAccessRequestFn` (`admin-access.ts:132-134`): "Managers can only decide team access requests". A manager sees a permanently dead control with no route forward and no indication of who *can* decide |
| IF-E2-35 | /admin/access | `src/routes/admin.access.tsx:229-281` renders only a profile select and two tab buttons | local-only switch representing persisted state | UNAVAILABLE | frontend | Add a `requestStatus` select bound to `updateSearch` | `adminAccessSearchSchema.requestStatus` (`schemas.ts:174-177`) supports five values, `accessRequestsQueryKey` keys on it (33-34) and `requestsQueryOptions` forwards it (46) — the entire read path is wired, with no control anywhere to change it. The queue is permanently pinned to `pending` unless the URL is hand-edited, which is also what masks IF-E2-33 |
| IF-E2-36 | /admin/audit | "Export audit" `src/components/admin/admin-audit-table.tsx:24-30` → `onExport` `src/routes/admin.audit.tsx:187` → `exportAudit` `83-99` → `exportAdminAuditLogsFn` `src/server-functions/admin-access.ts:195-201` | export without artifact | **REAL** | backend | Have `exportAdminAuditLogsFn` ignore `page`/`limit` (or stream all matching rows), and emit CSV through the `src/lib/csv.ts` serializer repo-map §Libraries schedules for E4 | **What it emits, exactly:** `exportAdminAuditLogsFn` → `requireCapability("audit.export")` → `listAdminAuditLogs(input)`, returning `Paginated<AdminAuditLog>`. The route takes `result.items`, `JSON.stringify(items, null, 2)`, builds a `Blob({type:"application/json"})`, `URL.createObjectURL`, creates a detached `<a download="fimmick-admin-audit.json">`, calls `.click()`, then `URL.revokeObjectURL`. So: **JSON, client-side Blob + synthetic anchor click, filename `fimmick-admin-audit.json`.** It **is** REAL — it reaches an authorized server export function and a file does land. The defect is scope: `auditFilters(search)` pins `limit: 50` and `page: search.page` (`admin.audit.tsx:41-52`), so the "export" is only the 50 rows currently on screen, silently, under a filename that claims to be the audit history. This is also the repo's only control labelled "export" and it emits JSON, not CSV (repo-map §Libraries). Secondary: `URL.revokeObjectURL` fires synchronously after `.click()` (94), which races the download in some browsers |
| IF-E2-37 | /admin/audit | `src/components/admin/admin-audit-table.tsx:23-31`; `onExport` passed unconditionally at `src/routes/admin.audit.tsx:187` | disabled action without explanation | REAL | frontend | Pass `canExport` derived from the actor's `audit.export` grant, and add a submitting flag | The button renders whenever `onExport` exists, with no capability check and no in-progress state. `ROLE_GRANTS` gives `audit.export` only to `super_admin` and `admin` (`policy.ts:30-31`) — but an explicit `audit.view` allow-override (which IF-E2-32 makes trivially easy to mint unscoped) produces an actor who can read the page and gets a live Export button that always fails. Repeated clicks fire concurrent exports |
| IF-E2-38 | /admin/audit | `src/routes/admin.audit.tsx:97` `toast.error(error instanceof Error ? error.message : ...)` | raw database or driver error shown to users | REAL | frontend | Route through the B5 sanitizer | Same defect as IF-E2-16 and IF-E2-47. `requireCapability("audit.export")` runs four raw SQL queries via `loadAuthorizationContext` (`authorization.server.ts:38-80`); a driver failure toasts its verbatim message |
| IF-E2-39 | /admin/audit | `src/routes/admin.audit.tsx:66-67` `const [from, setFrom]` / `const [to, setTo]`, seeded from `search.from`/`search.to`, with no rendered input | local-only switch representing persisted state | UNAVAILABLE | frontend | Render two `datetime-local` inputs into the filter grid and include them in `submitFilters` | Dead state. `adminAuditSearchSchema` declares `from`/`to` as ISO datetimes (`schemas.ts:188-189`), `auditFilters` forwards them (47-48) and `auditSchema` accepts them server-side (`admin-access.ts:50-51`) — the whole path works, and the two setters are never called. Date-range filtering is the single most obvious thing to want from an audit log |
| IF-E2-40 | /admin/audit | `src/components/admin/admin-audit-table.tsx:49-51` — `{entry.created_at}` rendered raw | raw database or driver error shown to users | READ-ONLY | frontend | Use `formatDateTime` from `src/lib/format.ts` | Emits the raw Postgres timestamp string. Every sibling admin component uses the SSR-safe formatter (`admin-overview.tsx:158`, `people-directory.tsx:244`, `user-detail-panel.tsx:75`, `organization-unit-detail.tsx:152`), and `CLAUDE.md` requires it. Only the audit table opts out — on the one screen where timestamps are the point |
| IF-E2-42 | /account | `src/routes/account.tsx:25` registered; `src/components/app-sidebar.tsx:43-68` (no entry), `170-191` (footer is avatar + sign-out only) | "coming soon" as active navigation | UNAVAILABLE | frontend | Link the sidebar-footer avatar to `/account` | `grep -rn 'to="/account"\|href="/account'` over `src` returns **nothing**. The only inbound path in the whole product is `redirect({href:"/account?welcome=1"})` from `invite.$token.complete.tsx:16`, which fires exactly once, at invitation acceptance. After that first visit an existing user has no way to reach their own profile, availability, delegation or access-request screen — six working server-backed writes with no door |
| IF-E2-43 | /account | `src/routes/account.tsx:25-31` has no `validateSearch`; `src/routes/invite.$token.complete.tsx:16` sends `?welcome=1` | unscoped or capability-inappropriate navigation | READ-ONLY | frontend | Add `validateSearch` with `welcome: z.coerce.boolean().optional().catch(undefined)` and render a first-run panel, or drop the param from the redirect | The redirect states a contract — a welcome state for a newly activated user — that the destination does not implement or even parse. The brand-new user lands on a bare "Account settings" tab strip with no onboarding cue |
| IF-E2-44 | /account | Capability field `src/components/account/account-settings.tsx:602-607` — free-text `<input>` seeded `"accounts.update"` | raw database or driver error shown to users | REAL | frontend | Replace with a `<select>` over `CAPABILITIES` minus `NON_REQUESTABLE_CAPABILITIES` | The server validates against `capabilitySchema` = `z.enum(CAPABILITIES)` (52 values, `types.ts:16-63`). A typo throws a `ZodError` whose message reaches `runMutation`'s catch (`account.tsx:62`) and is toasted verbatim. `PermissionOverrideDialog` already renders exactly the right `<select>` at `permission-override-dialog.tsx:125-136`. The UI also gives no hint that `permissions.override` is rejected by `NON_REQUESTABLE_CAPABILITIES` (`schemas.ts:98`, 119-125) |
| IF-E2-45 | /account | Team ID field `src/components/account/account-settings.tsx:612-617` | disabled action without explanation | REAL | frontend | Populate a `<select>` from `getAdminOrganizationFn()` or the account's visible teams | `accessRequestSchema` requires `z.uuid()` (`schemas.ts:103`). The user is asked to type a team UUID they have no way to look up: `/admin/teams` is admin-gated and the account's own Teams list (`account-settings.tsx:355-366`) shows names, not ids |
| IF-E2-46 | /account | Delegate profile field `src/components/account/account-settings.tsx:433-437` | disabled action without explanation | REAL | frontend | Populate a `<select>` of active profiles | Same shape as IF-E2-45: a raw profile id typed by hand. `createMyDelegation` then does the right thing (`account.ts:186-192`: rejects self, rejects non-active) but every rejection costs a full round-trip and surfaces as a generic string |
| IF-E2-47 | /account | `src/routes/account.tsx:62` `toast.error(error instanceof Error ? error.message : "Could not update your account")` | raw database or driver error shown to users | REAL | frontend | Route through the B5 sanitizer | All six account writes funnel through this one catch. Zod validation messages, `AdminError` messages and Neon driver errors from `src/server/repositories/account.ts` all surface verbatim to the end user |
| IF-E2-48 | /account | Save buttons `src/components/account/account-settings.tsx:339-345`, `394-400`, `470-476`, `563-569`, `631-637`, plus "Cancel delegation" `502-508` | replay/retry without idempotency | REAL | frontend | Add a per-action submitting flag and `disabled`, matching `user-role-dialog.tsx:154` / `organization-unit-dialog.tsx:247` | Not one of the six has an in-progress state. Double-clicking "Submit access request" writes two rows via `createAccessRequest`; double-clicking "Create delegation" writes two overlapping delegations. Each also produces two success signals — `runMutation`'s `toast.success` (`account.tsx:48`) *and* the component's own `role="status"` line — while a failure produces a raw toast plus a contradictory generic `role="status"` string, because the component swallows the error into its own message (e.g. `130-132`, `154-156`) |
| IF-E2-49 | /account | `securityContent` prop declared `src/components/account/account-settings.tsx:52`, rendered `408-412`; never supplied by `src/routes/account.tsx:69-89` | disabled action without explanation | UNAVAILABLE | frontend | Pass the Neon Auth security controls, or remove the prop | The "Neon Auth security controls" slot never renders, so the Security tab is only a `<a href="/login/forgot-password">` and the app-session revoke. MFA, device and password-change controls have a designed slot and no occupant |
| IF-E2-50 | /account | Tab strip `src/components/account/account-settings.tsx:248-263` — `onClick={() => setTab(item.id)}` | local-only switch representing persisted state | READ-ONLY | frontend | Move to `validateSearch` + `navigate({search})` as `admin.people.$id.tsx:81-101` and `admin.access.tsx:263-279` do | Five tabs held in local `useState`. Not deep-linkable, lost on reload, and not restorable — so IF-E2-42's single inbound link always drops the user on "Profile". Every other tabbed route in the slice persists tab state to the URL |
| IF-E2-51 | /notifications | `onClick={markAllRead}` `src/routes/notifications.tsx:114`; `onClick={() => markAsRead(n.id)}` `186`; both rethrow from `src/hooks/use-notifications.ts:101` and `115` | success toast without a server action | REAL | frontend | Wrap both bindings in a handler that catches and toasts, and disable while in flight | The hook is careful — token-guarded optimistic write with real rollback (`use-notifications.ts:68-92`) — and then rethrows into a bare `onClick`. Result: an unhandled promise rejection, no toast, no `role="alert"`. The row visibly flips to read and then silently flips back, which reads as a UI glitch rather than a failure. No in-progress state either, so repeat clicks fire repeat writes |
| IF-E2-52 | /notifications | `<Link to={notificationLink(n) as never}>` `src/routes/notifications.tsx:192`; `notificationLink` `65-71` | unscoped or capability-inappropriate navigation | REAL | frontend | Return typed `to`/`params` pairs and delete the cast; give `engagement` a real target | The `as never` cast disables the router's link type-checking for every "Open" button on the page. Behind it, `object_type === "engagement"` discards `n.object_id` and sends every engagement notification to the unfiltered `/renewals` list, and the default branch links `/notifications` back to itself — an "Open" button that goes nowhere |

**Policy modules a later step must prove untouched** (identified per the slice instruction; none of the fixes above may weaken them): `src/lib/admin/policy.ts` — `ROLE_GRANTS` (29-137), `overrideIsActive` (139-155), `managerCanTarget` (157-189, incl. the unowned-resource deny at 186), `evaluateAuthorization` (191-236, incl. `invalid_target` 201-208 and `protected_role` 209-215); `src/lib/admin/types.ts` — `USER_ROLES`, `PROFILE_STATUSES`, `CAPABILITIES`; `src/lib/admin/schemas.ts` — `nonEmptyReasonSchema` (7), `NON_REQUESTABLE_CAPABILITIES` (88-98) and its `superRefine` (119-125); `src/server/auth/authorization.server.ts` — `loadAuthorizationContext` / `requireCapability` / `requireAnyCapability` (192-207); `src/server/auth/resource-ownership.ts` (VF-7, authorization hot path); `src/server-functions/admin-users.ts` — `assertCanAssignRole` (98-105) and `profileTarget` (160-173); `src/server-functions/admin-invitations.ts` — `assertCanAssignRole` (46-53), `requireInvitationTargets` (72-76), `authorizeStoredInvitation` (153-171); `src/server-functions/admin-access.ts` — `assertDeciderCanGrant` (106-120) and the segregation-of-duties check (137-139); `src/server/admin/reassignment.server.ts`.

## REVIEWED-OK

**`/admin` (`admin.tsx`)** — `AdminShell` nav `<Link>`s (`admin-shell.tsx:52-65`): real, capability-filtered server-side by `getAdminNavigationFn` (`admin-users.ts:119-137`), `aria-current="page"` set correctly, and the `/admin` vs `/admin/` exact-match exception at 47-50 is right. `<Outlet/>` at 33 present and correct.

**`/admin/` (`admin.index.tsx`)** — parallel `Promise.all` in one queryFn under one key (14-20) is sound; `getAdminAuditSummaryFn` degrading FORBIDDEN to `[]` (`admin-access.ts:188-192`) is the right call for a summary strip; empty state at `admin-overview.tsx:163-167` present; `formatCount`/`formatDateTime` used; `tabular-nums` on values (65).

**`/admin/people`** — debounced search input (`people-directory.tsx:116-124` + effect 63-75, 250 ms, `latestSearch` ref avoids a stale closure): real, reaches `getAdminUsersFn`. Status select (126-137) and role select (138-152): real, both forwarded by `toUserFilters`. Row click + Enter/Space keydown (210-220): real, drives `adminUserQuery`. Previous/Next (277-294): real, correctly `disabled` on `hasPrevious`/`hasNext`. "Invite users" (97-104): real, correctly gated by `canInvite`. "Manage lifecycle" (`user-detail-panel.tsx:90-96`): real, correctly gated by `canManageLifecycle` *and* `status !== "deactivated"`. Invite dialog role select, email textarea, cancel and close: real. Lifecycle dialog suspend/deactivate toggles (141-172), reason textarea (204-210), submit (230-240 — `disabled={!canSubmit || submitting}`, correct), cancel/close: real, with genuine error surfacing at 213-220. Successor selects (`work-reassignment-table.tsx:73-86`): real, correctly filtered to active non-self candidates (30-32). Role dialog reason textarea, save (152-159, `disabled={submitting}`), cancel, close: real. `openLifecycle`'s `lifecycleRequest` race counter (`admin.people.tsx:152, 162, 168, 176`) is a correct out-of-order guard. Forbidden panel `role="alert"` (211-220): correct.

**`/admin/people/$id`** — tab `onValueChange` → `navigate({search, replace:true})` with `profile` normalised to `undefined` (81-91): correct URL-state handling, no server call needed. Teams tab (119-140), Work tab (141-157), Activity tab (177-188): real persisted data, correctly read-only, `tabular-nums` on workload counts (150). Not-found panel `role="status"` (59-68) and forbidden panel `role="alert"` (49-58): both correct. *(All of this is unreachable today per IF-E2-01 — reviewed on its merits for after the Outlet fix.)*

**`/admin/teams`** — Departments/Working-teams kind tabs (`organization-directory.tsx:100-129`): real, `role="tab"` + `aria-selected`, correctly clear `unit`. Status select (78-93): real client-side filter over a fully-loaded directory. Unit list buttons (194-209): real, `aria-pressed` set. "Edit" (`organization-unit-detail.tsx:70-78`): real, correctly gated on `onEdit && canManage`. Unit tabs (87-103): correct URL state. Overview tab (108-156) and Work tab (180-193): real persisted data. Department-members explainer (161-167): honest — department membership genuinely is modelled through teams. Unit dialog name/description/status/head/deputy/default-owner selects and the archive-confirmation checkbox (`organization-unit-dialog.tsx:156-228`): all real, `ProfileSelect` correctly filters to active users (281), and the archive gate at 81-84 with the open-work count at 223 is a good destructive-action guard. Submit (245-251) has both `disabled={submitting}` and a real catch. `loadUsers`/`loadUnit` swallowing FORBIDDEN to `[]`/`null` (44-73): defensible for a side panel.

**`/admin/teams/$id`** — "Back to organization" `<Link>` (205-217) correctly reconstructs the parent's search state. Not-found panel (194-200). *(Unreachable today per IF-E2-03.)*

**`/admin/access`** — Profile `<select>` (`admin.access.tsx:244-257`): real, drives `overridesQueryOptions`. Requests/Effective tab buttons (263-279): correct URL state, `role="tab"` + `aria-selected`. "Create override" (300-307): real, correctly gated on `super_admin` — and `canCreateOverride` is re-checked inside the dialog (`permission-override-dialog.tsx:108-114`), so the gate is defence-in-depth rather than a single client check. Approve/Reject buttons (`access-request-queue.tsx:140-154`), decision reason textarea (163-172), "Temporary access" checkbox (177-187), expiry input (195-206), submit (216-223 with `disabled={submittingId === request.id}` and a real catch at 79-84): all real, reaching `decideAdminAccessRequestFn`. Empty state (35-41). Override dialog capability select (built from `CAPABILITIES`, 131-135), effect select, reason, temporary checkbox, expiry, submit: all real. `OverrideHistory`'s active/expired/revoked split (`admin.access.tsx:92-97`, 166-168) matches `overrideIsActive` in `policy.ts:145-149` — the client and the policy engine agree, which is what matters here. `refreshAdminAccessCaches`'s cache-scan for `scope==="access-requests"` and `scope==="audit"` keys (171-187) is unusual but correct, and is the only place in the slice that invalidates the audit list after a decision.

**`/admin/audit`** — Actor / target-type / target / action inputs (129-161) and severity select (165-175): real, all five reach `getAdminAuditLogsFn` via `auditFilters`. "Apply filters" submit (177-182): real, correctly resets `page: 1`. Previous/Next (`admin-audit-table.tsx:87-102`): real, correctly `disabled` at both bounds. Per-row `<details>/<summary>` "View change" (63-75): correct native disclosure, no JS needed, shows the real before/after snapshots. Forbidden panel (101-110). `auditQueryOptions` catching FORBIDDEN inside the queryFn rather than the loader (18-30) is a good pattern — it survives refetches, not just the first load.

**`/account`** — Name / job title / phone / avatar inputs (303-336) and "Save profile" (339-345): real → `updateMyProfile`, and correctly the only write that also invalidates `crmQueryKeys.shell()` (`account.tsx:72`), since the sidebar shows the name. "Reset password" `<a href="/login/forgot-password">` (382-385): real destination — `/login/$authPath` accepts it. "Revoke app sessions" (394-400): real → `revokeMyAppSessions` → `setSessionInvalidBefore`, and the copy at 389-392 honestly states that the current session survives until refresh. Availability select + leave start/end (528-559) and "Save availability" (563-569): real → `updateMyAvailability`, with a correct client-side end-after-start pre-check (160-163) mirroring the server's `superRefine` (`account.ts:54-71`). Delegation start/end/reason inputs (442-466), "Create delegation" (470-476), "Cancel delegation" (502-508, correctly shown only for `delegatorProfileId === profile.id && status === "active"`): real. Request-type select (589-596) and reason textarea (623-628): real. Teams list (355-366), workload cards, request history (~645-660): real read-only persisted data. `formatDelegationDate` (75-78) correctly uses the pinned `en-GB`/UTC formatter, with the hydration rationale documented in-file.

**`/notifications`** — Six filter tab buttons (123-141): real URL state via `navigate({search, replace:true})`, filtering an already-fully-loaded list client-side — correct, no server round-trip warranted. Unread-count `Badge` (135-139): real, from `getNotifications`'s server-side `countUnreadNotifications`. "Mark all read" correctly hidden when `unreadCount === 0` (113) and "Mark read" correctly hidden on already-read rows (185). Empty state (146-150). `formatDateTime` + `useClientNow`-guarded `relativeTime` (178-181): the right SSR-safe pattern. The `useNotifications` optimistic machinery itself (`use-notifications.ts:49-119`) — `cancelQueries` before the optimistic write, per-notification `Symbol` tokens so a slow request cannot clobber a newer one, exact-key invalidation after settle — is the strongest mutation code in this slice and is the model the shared helper should be built from.

## PATTERNS

| Route | Header | Metrics | Filters | Table or list impl | Empty state | Loading state | Error state | Status badge | Detail panel | Action placement |
|---|---|---|---|---|---|---|---|---|---|---|
| `/admin` | **neither** — page-local aside header, `admin-shell.tsx:32-42` | missing | missing | page-local nav `<Link>` list, `admin-shell.tsx:44-68` | missing | missing | missing (falls to root `errorComponent`, `__root.tsx:53-81`) | missing | missing | missing (layout only) |
| `/admin/` | **neither** — page-local, `admin-overview.tsx:79-100` | page-local `MetricLink`, `admin-overview.tsx:43-74` — **6 tiles, exceeds the four-primary cap**; shared `MetricStrip` (VF-6) unused | missing | page-local anchor list, `admin-overview.tsx:141-162` | page-local, `admin-overview.tsx:163-167`; shared `WorkSurfaceEmpty` unused | missing | missing | missing | missing | header-right `<a>`, `admin-overview.tsx:92-98` |
| `/admin/people` | **neither** — page-local, `people-directory.tsx:86-106` | missing | page-local, `people-directory.tsx:109-166` (debounced search + 3 selects) | page-local `<table>`, `people-directory.tsx:178-262`; no shadcn `Table` | page-local, `people-directory.tsx:200-205` | **missing at route level** — the `loading` prop exists (`people-directory.tsx:194-199`, `user-detail-panel.tsx:20-25`) and `admin.people.tsx:225-233` never passes it; `lifecycleLoading` is page-local and mis-rendered (IF-E2-14) | page-local `role="alert"`, `admin.people.tsx:211-220`; the `error` prop at `people-directory.tsx:168-175` is never passed | page-local capitalize text `people-directory.tsx:242` + page-local pill `user-detail-panel.tsx:43-45`; shared `StatusBadge` unused | page-local `UserDetailPanel` aside, `admin.people.tsx:234-245` — **`hidden md:block`, so it disappears below `md`**; shared `SalesContextPanel` unused | header-right (`people-directory.tsx:96-105`) + panel footer (`user-detail-panel.tsx:79-107`) + inert row-right (`people-directory.tsx:247-255`) |
| `/admin/people/$id` | **neither** — page-local, `admin.people.$id.tsx:73-79` | page-local workload grid, `admin.people.$id.tsx:144-155` | missing | page-local `<ul>` (teams, 123-135) + `<dl>` (security, 161-174) | page-local, `admin.people.$id.tsx:137` and `59-68` | missing | page-local `role="alert"` 49-58 + `role="status"` 59-68 | missing (plain capitalize text, 164) | shared-in-repo `UserDetailPanel` reused inside the Profile tab, 105 | **missing — no actions at all on the route** |
| `/admin/teams` | **neither** — page-local, `organization-directory.tsx:42-63` | page-local `Metric`, `organization-unit-detail.tsx:219-227` (Work tab only) | page-local, `organization-directory.tsx:64-94` (undebounced search + status select + kind tabs) | page-local `<button>` list `organization-directory.tsx:190-212` + page-local `<table>` `team-member-table.tsx:144-198` | page-local ×3: `organization-directory.tsx:191-192`, `team-member-table.tsx:140-141`, `organization-unit-detail.tsx:39-44` | **missing** — `loading` prop exists (`organization-directory.tsx:133-134`), never passed by `admin.teams.tsx:278-287` | **missing** — FORBIDDEN swallowed to `[]`/`null` inside `loadUsers`/`loadUnit` (44-73), so a scope failure is indistinguishable from an empty org | page-local capitalize text, `organization-directory.tsx:206-208` and `organization-unit-detail.tsx:68` | page-local `OrganizationUnitDetail` aside, `admin.teams.tsx:288-298` | header-right (`organization-directory.tsx:55-62`) + panel header-right (`organization-unit-detail.tsx:70-78`) + inline row (`team-member-table.tsx:186-192`) |
| `/admin/teams/$id` | **missing** — no title anywhere; only a back `<Link>`, `admin.teams.$id.tsx:204-218` | page-local (via `OrganizationUnitDetail`) | missing | page-local (via `OrganizationUnitDetail` / `TeamMemberTable`) | page-local, `admin.teams.$id.tsx:194-200` | missing | **missing** — no `AdminError` catch; raw message via root `errorComponent` (IF-E2-04) | page-local capitalize text | page-local `OrganizationUnitDetail` used as the whole page body | panel header-right + inline row (inherited) |
| `/admin/access` | **neither** — page-local, `admin.access.tsx:231-260` | missing | page-local, profile `<select>` only (244-257); no `requestStatus` control (IF-E2-35) | page-local `<table>` ×2 (`effective-access-table.tsx:44-85`, `admin.access.tsx:110-133`) + page-local `<article>` cards (`access-request-queue.tsx:104`) | page-local ×3: `access-request-queue.tsx:35-41`, `admin.access.tsx:106-107`, `318-321` | missing | page-local `role="alert"`, `admin.access.tsx:218-227`; per-action errors in `access-request-queue.tsx:211-215` | page-local **hardcoded** "Pending" pill, `access-request-queue.tsx:125-127` (IF-E2-33) | page-local inline sections, not an aside — `EffectiveAccessTable` + `OverrideHistory` (309-316) | header-right (`admin.access.tsx:299-307`) + per-card (`access-request-queue.tsx:130-155`, 216-223) |
| `/admin/audit` | **neither** — page-local, `admin.audit.tsx:114-122` | missing | page-local **raw `<input>`/`<select>`**, `admin.audit.tsx:123-183` — the only filter bar in the slice bypassing shadcn `Input`/`Select` | page-local `<table>`, `admin-audit-table.tsx:34-81` | **missing** — an empty result renders headers over an empty `<tbody>` with no message | missing | page-local `role="alert"`, `admin.audit.tsx:101-110`; raw toast at 97 | page-local capitalize text, `admin-audit-table.tsx:58` | missing — per-row `<details>` disclosure instead (63-75) | section header-right Export (`admin-audit-table.tsx:23-31`) + inline form submit (`admin.audit.tsx:177-182`) + footer pagination (`admin-audit-table.tsx:86-102`) |
| `/account` | **neither** — page-local, `account-settings.tsx:232-241` | page-local workload cards, `account-settings.tsx:416-424` | missing | page-local `divide-y` lists (teams 355-366, delegations 481-510, requests ~645-660) | page-local ×3: "No active teams." (357), "No current delegations." (483), "No access requests yet." (~648) | **missing** across all six writes | page-local `role="status"` strings (347, 402, 478, 571, 639) **plus** a raw toast from the route (`account.tsx:62`) — two competing channels, and they can disagree | missing (plain capitalize text) | missing — five local tabs instead (248-263) | bottom-of-section buttons: `account-settings.tsx:339`, `394`, `470`, `563`, `631` |
| `/notifications` | **shared component `PageHeader`** — `notifications.tsx:109-120` (the only route in this slice using a shared header; VF-4) | missing | page-local pill tabs, `notifications.tsx:122-142` | page-local `div` list, `notifications.tsx:144-199` | page-local, `notifications.tsx:146-150`; shared `WorkSurfaceEmpty` unused | missing | **missing** — rejections are unhandled (IF-E2-51) | shared `Badge` for the unread count (136-139) + page-local `typeColor`/`typeIcon` maps (51-63); shared `StatusBadge` unused | missing | shared `PageHeader` `actions` slot (112-119) + per-row right (184-194) |

**Cross-cutting:** nine of ten routes use **neither** shared header — `/notifications` is the lone `PageHeader` consumer, and `/admin/teams/$id` has no header at all. Not one route in this slice uses `MetricStrip`, `WorkSurfaceEmpty`, `SalesContextPanel` or `StatusBadge`; every metric tile, empty state, detail panel and status pill here is page-local, which is the strongest concrete evidence for VF-4/VF-5/VF-6 in the admin surface. **Loading state is missing on all ten routes** — no `pendingComponent` anywhere, and three components (`PeopleDirectory`, `OrganizationDirectory`, `UserDetailPanel`) expose a `loading` prop that no route passes. Error state is the most divergent cell in the table: four routes render a `role="alert"` forbidden panel, one swallows scope failures into empty arrays, one has no handling at all, one mixes `role="status"` with raw toasts, and one leaks unhandled rejections.