## FALSE-REAL

| # | Auditor verdict challenged | Element (file:line) | What I found opening both files | Corrected verdict |
|---|---|---|---|---|
| FR-1 | IF-C1 (`/leads/$id`) marks **Qualify** REAL | `handleQualifyLead` `src/routes/leads.$id.tsx:159-162`; button wired from the same file | `await triggerLeadAgent({data:{leadId}})` then `toast.success("Qualification agent queued")` on the very next line. The return value is never read. `triggerLeadAgent` (`src/server-functions/leads.ts:140`) returns `{triggered:false, reason:"missing_webhook"}` at `leads.ts:157-158` and `{triggered:false, reason:"already_running"}` at `leads.ts:172` — it does **not** throw. No try/catch, no in-flight guard, no invalidation. | REAL-but-lying → the exact case the brief says to flag. Needs `if (!result.triggered)` branching before the toast. |
| FR-2 | IF-C1 (`/leads/$id`) marks **Generate Quote** REAL | `handleGenerateQuote` `src/routes/leads.$id.tsx:147-150` | Identical: `await triggerQuoteAgent(...)`; `toast.success("Quote agent triggered — quote will appear shortly")`. `triggerQuoteAgent` (`src/server-functions/quotes.ts:134`) returns the sentinel at `quotes.ts:151-152` and `already_running` at `quotes.ts:166`. | Same as FR-1. **These two are the only non-branching AI call sites in the repository** (see FR-3). |
| FR-3 | The **brief's own established fact** ("accounts.$id.tsx and leads.$id.tsx do NOT [branch]") | `runRelationshipIntelligence` `src/routes/accounts.$id.tsx:154-186` | `accounts.$id` **does** branch: `if (!result.triggered)` at `:166`, with `already_running` at `:167-169` and `toast.error("Relationship intelligence webhook is not configured")` at `:170`. It also has an in-flight guard (`:155-158`), try/catch (`:186`) and a `finally` reset. IF-D1's correction is right and the brief is half wrong. I enumerated all seven trigger call sites (`grep triggerLeadAgent\|triggerQuoteAgent\|triggerLeadReplyDraft\|triggerRiskScoreAgent\|triggerRelationshipIntelligence src --include=*.tsx`): branching = `index.tsx:152,170,188`, `accounts.$id.tsx:164`, `renewals-preview-panel.tsx:89`; non-branching = `leads.$id.tsx:148,160`. | Five branch, two do not — both in `leads.$id.tsx`. |
| FR-4 | IF-D1-01 marks `/accounts` **Sort select** REAL | `<select id="account-sort">` `src/routes/accounts.tsx:227-252` | The loader at `accounts.tsx:29-38` passes `getAccountsIndexRead({data:{lifecycle_stage: search.lifecycle, page: search.page, limit: search.limit}})` — `search.sort` is **never sent**. The only server effect is that `crmQueryKeys.accounts.list(search)` (`:32`) includes `sort`, so a new cache key refetches byte-identical rows; ordering happens in the client `useMemo`. A control that reaches a server function while changing nothing about what the server returns is not REAL under the stated decision order. | READ-ONLY (local-only sort over the loaded page). IF-D1's own note contradicts its verdict. |
| FR-5 | IF-C2 marks all `/quotes/$id` lifecycle buttons REAL and uniformly guarded | Header **Submit for approval** `src/routes/quotes.$id.tsx:440-443`; handler `handleRequestApproval` `:302-307` | It is the one lifecycle button with **no `disabled={saving}`** — every sibling has it (`:447`, `:450`, `:456`, `:461`). The handler never calls `setSaving`, has no try/catch, and toasts success unconditionally after `requestQuoteApproval`. A throw is an unhandled rejection with zero user feedback; a double-click re-enters. Contrast `handleSubmitForApproval` `:280-299`, which does all of it correctly. | REAL, but needs its own finding: missing in-progress state + missing failure feedback + double-submission on a lifecycle transition. |
| FR-6 | IF-C2 folds `/quotes/new` submit into the generic IF-C2-35/36 rows | `submit` `src/routes/quotes.new.tsx:227-292`; button `:742` `<Button size="sm" onClick={submit}>` | No `disabled`, no in-flight state, and `await createQuote({data: payload})` at `:257` has **no try/catch** (only the nested `requestQuoteApproval` does, `:268-277`). A second click persists a second `quotes` row and a second `human_approvals` row. | REAL but unguarded; the duplicate here is a persisted financial document, so it is not the same severity as the generic row. |
| FR-7 | IF-C1 treats `/` handlers as uniformly wired | `confirmMove` `src/routes/index.tsx:113-135`, `moveLead` `:138-147`, `createFollowUpTask` `:208-219` | All three are genuinely REAL (`moveLeadStage` `leads.ts:108`, `createTask` `tasks.ts:49`) but have **no try/catch**, while the three AI handlers on the same page do (`:150-203`). A failed stage move toasts success at `:124` before `refreshDashboard`… no — it toasts, then awaits; a rejection from `moveLeadStage` skips the toast but produces no error either. | REAL with missing failure feedback; inconsistent within one file. |

## FALSE-WRITEOFF

| # | Auditor write-off | Element (file:line) | Server path that already exists | Corrected verdict |
|---|---|---|---|---|
| FW-1 | `/quotes/new` **Save draft** — IF-C2 PARITY records only "Save draft writes nothing" | `src/routes/quotes.new.tsx:739-741`, `onClick={() => toast.message("Draft saved")}` | `createQuote` (`src/server-functions/quotes.ts:106-112`) is **already imported and called in this same file** at `:257`. The repository insert hard-codes the status: `src/server/repositories/quotes.ts:243-246` — `insert into quotes (…, status, …) values ($1,…,'draft',…)`. "Save draft" is literally `submit` minus `requestQuoteApproval`. | Not UNAVAILABLE. Frontend-only wiring, zero backend work. |
| FW-2 | IF-C2-01 `/quotes` **Duplicate** → "UNAVAILABLE, backend then frontend: add a `duplicateQuote` server fn" | `src/routes/quotes.tsx:250`, handler `duplicate` `:122-126` | No new server fn is needed. `CreateQuoteInput` (`src/server/repositories/quotes.ts:30-49`) already accepts `line_items`, `document_sections`, `cover_text`, `assumptions`, `payment_terms`, `quote_template_id`, `client_id`, `lead_id`, `account_id`, `total_value`, `valid_until`. `parent_quote_id` is in `editableQuoteUpdateColumns` (`:51-60`) and is **not** in `lifecycleQuoteUpdateFields` (`src/server-functions/quotes.ts:62-70`), so `updateQuote` (`quotes.ts:114`) can set it. Duplicate = `createQuote` + `updateQuote`, both exported and capability-checked today. | Owner is **frontend**, not "backend then frontend". |
| FW-3 | `/settings` **Team tab** — Invite / Role / Remove (in the truncated IF-E1-16…30 block) | `toast.message("Invite mocked")` `src/routes/settings.tsx:235`; role `<Select>` `:257-262` mutating local `APP_USERS` state seeded at `:227`; `toast.message("Mocked")` Remove `:278` | All three have working, capability-checked server functions that `/admin/people` already calls: `inviteUsers` (`src/server-functions/admin-invitations.ts:97`), `changeAdminUserRoleFn` (`src/server-functions/admin-users.ts:183`), `suspendAdminUserFn` (`admin-users.ts:204`) / `deactivateAdminUserWithReassignmentFn` (`:220`). A finished dialog exists at `src/components/admin/invite-users-dialog.tsx`. | Any "no server path" reasoning is wrong. Correct treatment is **REMOVED as a duplicate surface** (link to `/admin/people`), not a backend ask. |
| FW-4 | `/leads` **Import CSV** (IF-C1-10…15) | `src/routes/leads.tsx:235`, `toast.message("CSV import is mocked in this prototype.")` | Partial correction only: there is no lead-import server fn, but `createLead` (`src/server-functions/leads.ts:92`) exists and the repo already ships the two-phase CSV pattern twice — `validateClientImportRows`/`commitClientImportFn` (`src/server-functions/client-import.ts:21,30`) and `validateEventImportRowsFn`/`commitEventImportFn` (`src/server-functions/event-import.ts:32,45`), with server-side dedupe at `src/server/repositories/client-import.ts:44-99`. | UNAVAILABLE stands, but it is a *mirror of an existing module*, not an unknown design. |
| FW-5 | Write-offs whose premise ("no server path") is contradicted by exports with **zero UI callers** | — | Verified by scanning every `export const` in `src/server-functions/` for importers outside `src/server-functions/`, tests and same-named repository functions. Unwired writes: `updateClientContact` (`client-contacts.ts:35`), `createAccountContact`/`updateAccountContact` (`contacts.ts:23,34`), `createAccount`/`updateAccount` (`accounts.ts:57,65`), `updateCampaign` (`campaigns.ts:50`), `addCampaignMember` (`campaigns.ts:58`), `reactivateAdminUserFn` (`admin-users.ts:208`), `revokeAdminUserSessionsFn` (`:228`), `updateAdminUserFn` (`:175`), `resendUserInvitation` (`admin-invitations.ts:173`), `revokeUserInvitation` (`:186`), `revokeAdminPermissionOverrideFn` (`admin-access.ts:90`), `createAdminWorkDelegationFn`/`cancelAdminWorkDelegationFn` (`:155,:166`). Concretely: `/clients/$id` ships Add + Remove contact (`clients.$id.tsx:484,:498`) but no Edit although `updateClientContact` exists; `/accounts/$id` has **no** contact control at all (only `:234-238` Run intelligence, `:243` a Link, `:791/:816/:819` dismiss) although `createAccountContact`/`updateAccountContact` exist. | Not write-offs — missing controls over live server paths. Any note claiming the backend is the blocker for contact/account/campaign editing is wrong. |

**Write-offs I checked and confirmed correct** (do not reverse these): IF-C1-01 `/` Summarize — no `summar*` export anywhere in `src/server-functions/`; `ai-note-tidy.ts` exports only `tidyTouchpointNote`/`isAiNoteTidyAvailable`, which is note tidying, not timeline summary. IF-C3-05 `/approvals` assign reviewer — `src/server-functions/approvals.ts` exports only `getApprovals` and `decideApproval`; `bulkAssign` even says so at `approvals.tsx:305`. IF-E1-04 `/agents` Switch — there is no agent-config table in `neon/migrations/` (full table list checked); `status`/`human_approval` come from `AGENT_DEFINITIONS`, so READ-ONLY is right. `/settings` Pricing tab — `pricing_templates` (`neon/migrations/001_clientops_runtime.sql:91-99`) is a service price list (`service, unit_price, currency`), **not** approval thresholds; `pricingRules` (`settings.tsx:62`) has no persistence target. `/quotes` Archive — `QuoteStatus` (`src/lib/types.ts:10+`) has no `archived` member and `assertNoLifecycleQuoteUpdates` (`quotes.ts:72-80`) blocks `status` via `updateQuote`.

## MISSED

Caveat: the auditor output I was given is truncated for six of seven slices, so for items marked "†" I can only confirm they are absent from the visible text, not from the full table.

| # | Element (file:line) | Why it needs a verdict | Verdict I would assign |
|---|---|---|---|
| M-1 | `/agents/$name` **"Memory" tab trigger** — `agentHistorySearchSchema` tabs `["runs","memory","config"]` `src/routes/agents.$name.tsx:23-25`, `<Tabs onValueChange>` `:102`, content `:212-217` | A live, URL-persisted navigation target whose entire body is "Agent memory is not yet persisted." That is §16's "'coming soon' as active navigation" verbatim. IF-E1's visible narrative covers the Config tab controls, not this tab. † | REMOVED (hide the trigger until a memory store exists) |
| M-2 | `/quotes` **Archive** — `src/routes/quotes.tsx:251`, handler `archive` `:127-130` | Distinct from Duplicate and worse: `setRows(prev => prev.filter(...))` **deletes the row from the visible table** and toasts `"Quote archived"`. It reappears on the next loader run, so the user believes a destructive action succeeded and then sees it undone. IF-C2's PARITY groups it with Duplicate under one phrase; it needs its own row and category (local-only destructive action, not just a toast). | UNAVAILABLE — and the local `setRows` deletion must be removed immediately even before a server path exists |
| M-3 | `/leads` bulk **Assign** confirm — `src/routes/leads.tsx:721-724` | `onClick={() => { void onAssign(assignee); setAssignOpen(false); }}` — no empty-value guard, no `await`, dialog closes before the write settles. With the field untouched, `assignee` is `""` and `updateLead` is called once per selected id with `assigned_to: ""`. The raw Postgres uuid/FK error is what IF-C1 reports, but the *unguarded empty submit + fire-and-forget close* is a separate defect from the error text. † | REAL, needs validation + await + in-flight disable |
| M-4 | `/leads` **Owner filter** and the owner filter chip — `<Select value={owner}>` `src/routes/leads.tsx:323-329`; chip branch `:218-222`; `hasActiveFilters` `:231` | The Select's only child is `<SelectItem value="all">All owners</SelectItem>` — there is no second option, so `owner` can never leave `"all"`, which makes the owner chip at `:218-222` and its `clear` callback dead code. IF-C1 notes the filter "can never narrow" but does not give the dead chip branch a verdict. † | REMOVED (control) / dead code (chip) |
| M-5 | `/settings` product **Activate/Deactivate** — button `src/routes/settings.tsx:500`, handler `toggleActive` `:370-380` | REAL (`deactivateProductFn` `products.ts:40` / `updateProduct` `products.ts:32`), but it emits **no toast at all** on success or failure, and writes an optimistic `setQueryData` at `:375` with no rollback. Its sibling `create` `:351-368` does toast. † | REAL + missing success/failure feedback |
| M-6 | `/clients/$id` contact **Remove** — `remove` `src/routes/clients.$id.tsx:497-502` | REAL (`deleteClientContact` `client-contacts.ts:46`) but no confirmation, no toast, no try/catch, and it optimistically drops the row from local `rows` before the await resolves. Its sibling `create` `:483-495` toasts. | REAL + missing confirmation and failure feedback |
| M-7 | `/approvals` the **two Refresh buttons** — `src/routes/approvals.tsx:324-330` (header) and `:401-407` (empty state) | Both call `queryClient.invalidateQueries({queryKey: approvalsQueryKey, exact:true})`, which matches the component's active `useQuery` — genuinely REAL. Listed here only because neither appears in the visible IF-C3 table; they belong in a reviewed-OK list as evidence. † | REAL, reviewed-OK |

## INVALIDATION

The nine cache-bypassing routes, every mutation. **Correction to the framing first:** `ensureQueryData` vs. direct call is *not* the discriminator. A TanStack Router loader is not a React Query observer, so `invalidateQueries` refreshes a page only if the **component** holds a `useQuery` on the invalidated key. Six of the nine "bypassing" routes re-read through `useQuery` with `initialData`, so `invalidateQueries` works there; conversely two routes that *are* in the cache render `Route.useLoaderData()` and are broken. Both directions are below.

| Route | Mutation (file:line) | Invalidation | Verdict |
|---|---|---|---|
| `accounts/$id` | `dismissRelationshipSignalFn` `:133` | `invalidateCompanyWorkspaceMutation(...,"dismiss_relationship_signal")` → `["company-workspace",id,"overview"]` + `[...,"intelligence"]`, `exact:true, refetchType:"active"` (`src/lib/company-workspace/invalidation.ts:11,26-35`) | **CORRECT for the visible card** — `overview` is the active `useQuery` key at `accounts.$id.tsx:57-70`. Two defects: the `"intelligence"` key is **dead** (`src/lib/company-workspace/section-enablement.ts:11` sets `intelligence:false` and `getCompanyWorkspaceSectionEnablement` never enables it), and `"activity"` — the Timeline tab where a dismissal appears — is never invalidated. |
| `accounts/$id` | `triggerRelationshipIntelligence` `:164` | same helper, `"run_relationship_intelligence"` | **CORRECT / same two defects.** |
| `campaigns/$id` | `commitEventImportFn` `:152` | `campaigns.detail(id)` + `campaigns.section(id,"attendees")` + `accounts.lists()` + `contacts.lists()` (`:45-50`) | **CORRECT.** `campaigns.detail(id)` is the active `useQuery` key (`:73-78`); the filterless `section(id,"attendees")` → `["campaigns","detail",id,"section","attendees"]` is a strict prefix of the paged key (`src/lib/query-keys.ts:28-31`), so it matches the active attendee query (`:83-91`). |
| `campaigns/$id` | `createCampaignFollowUpTasksFn` `:187` | `follow_up_tasks` keys `:51-55` | **CORRECT.** |
| `clients/$id` | `createClientContact` `:484` | `crmQueryKeys.clients.section(clientId,"contacts")` `:479-481` | **CORRECT for the contacts panel** (active `useClientWorkspaceSection(clientId,"contacts")` at `:65`). **BROKEN for the loader-owned header and tab counts** — the route loads directly at `:40`, renders `Route.useLoaderData()`, and there is no `router.invalidate({filter})` anywhere in the file. |
| `clients/$id` | `deleteClientContact` `:498` | same | **Same split verdict.** |
| `job-sheets/$id` | `updateJobSheetPortions` `:202` | `getJobSheetMutationQueryKeys` (`src/lib/job-sheet-editor.ts:208-229`), includes `jobSheets.detail(id)` | **CORRECT** — `jobSheets.detail` is the active `useQuery` key with a real `queryFn` (`:96-100`). |
| `job-sheets/$id` | `acceptJobSheetForAccounting` `:237` | same | **CORRECT.** |
| `job-sheets/$id` | `updatePortionXeroReference` `:265` | same | **CORRECT.** |
| `leads/$id` | `updateLead` (`handleStatusChange` `:152-157`) | `invalidateLeadMutation(...,"status_change")` → `leads.detail(id)` + `leads.lists()` (`:90-107`) | **CORRECT** — `leads.detail(leadId)` is the active `useQuery` key (`:113-120`). Only the `head` title, built from `loaderData` (`leads.$id.tsx` head), goes stale. |
| `leads/$id` | `triggerLeadAgent` `:160` | **none** | **BROKEN** — no invalidation at all. Masked only by `refetchInterval: 12_000` at `:118`, i.e. up to 12 s of stale qualification state after a success toast that was itself unconditional (FR-1). |
| `leads/$id` | `triggerQuoteAgent` `:148` | **none** | **BROKEN** — same, and the Related-quotes panel is what should change. |
| `quotes/$id` | `handleSaveDraft` `:268`, `handleSubmitForApproval` `:280`, `approveAndIssueReviewedQuote` `:255`, `handleRequestApproval` `:302`, `handleRejectQuote` `:311`, `handleApproveQuote` `:328`, `handleIssueQuote` `:347`, `handleAcceptQuote` `:361` | all via `invalidateQuoteMutation` `:134-144`, whose key sets include `quotes.detail(id)`, `quotes.section(id,"versions")`, `quotes.section(id,"document")`, `jobSheets.lists()` | **CORRECT (all eight).** `quotes.detail(id)` is the active `useQuery` key (`:150-155`); the filterless `section(id,"versions")` prefix-matches the paged versions query (`:166-172`). The `versions`/`document` queries are tab-gated (`enabled: search.tab === …`), so they refetch on next open — acceptable, not broken. |
| `quotes/$id_/pdf` | none | — | **N/A** — read-only route, only control is `window.print()`. |
| `quotes/new` | `createQuote` `:257` + `requestQuoteApproval` `:270` | `quotes.lists()`, `approvals.all()`, conditional `leads.detail(leadId)`, conditional `clients.section(clientId,"commercial")` (`:278-289`), then `navigate({to:"/quotes/$id"})` `:291` | **CORRECT.** Every key targets another route; this route is left behind, so it needs nothing. |
| `relationships` | `dismissRelationshipSignalFn` (via `relationship-command-center.tsx:64-66`), page callback `refreshAfterDismiss` `:44-46` | `crmQueryKeys.relationships.lists()` = `["relationships","list"]` | **CORRECT.** Prefix-matches the component's active `useQuery` on `relationships.list({page,limit})` (`:28-33`). IF-D2-02's correction to the VF-3 expectation is right — this is not a no-op. |

**Two routes outside the nine that fail the same way and belong on the list:**

| Route | Mutation | Verdict |
|---|---|---|
| `/renewals` | `triggerRiskScoreAgent` (`renewals-preview-panel.tsx:89`), `renewEngagement`/`endEngagement` (`mark-renewed-ended-dialog.tsx:34,37`), `createTouchpoint` (`touchpoint-logger.tsx:86`) — all refresh through `invalidateQueries` only (`renewals-preview-panel.tsx:54-62`) | **BROKEN.** The route *is* an `ensureQueryData` route (`renewals.tsx:72-78`) but the component renders `Route.useLoaderData()` at `:92` with **no `useQuery` and no `useRouter`** in the file. Loaders are not observers, so `invalidateQueries` cannot repaint the board. Needs `router.invalidate({filter: m => m.routeId === "/renewals"})`. |
| `/campaigns` | `createCampaign` (`campaigns.tsx:78`) | **BROKEN.** No invalidation of any kind and no `useQueryClient` in the file; it navigates to the detail. On return, the loader re-runs (`src/router.tsx` sets no `defaultStaleTime`) but `ensureQueryData` serves the cached page for `CRM_STALE_TIME_MS = 30_000` (`src/lib/performance/query-policy.ts:3,33`), so the new campaign is missing from the index for up to 30 s. |

## CONFIDENCE

I opened and read, in full or in the relevant ranges: all nine cache-bypassing route files, plus `index.tsx`, `leads.tsx`, `quotes.tsx`, `approvals.tsx`, `settings.tsx`, `agents.tsx`, `agents.$name.tsx`, `accounts.tsx`, `renewals.tsx`, `campaigns.tsx`, `reports.tsx`, `job-sheets.tsx`; the server functions behind every verdict I challenge (`quotes.ts`, `leads.ts`, `accounts.ts`, `approvals.ts`, `admin-users.ts`, `admin-invitations.ts`, `products.ts`, `client-contacts.ts`); the repositories `quotes.ts` (create/update column sets and the lifecycle guard); `query-keys.ts`, `query-policy.ts`, `router.tsx`, both workspace-section hooks, `company-workspace/invalidation.ts` and `section-enablement.ts`; and the full `create table` inventory in `neon/migrations/`. I also ran an exhaustive reachability scan of every `export const` in `src/server-functions/` against non-test, non-repository importers, which is what produced FW-5. That gives me high confidence in **FALSE-REAL 1–5**, **FALSE-WRITEOFF 1–3 and 5**, and the entire **INVALIDATION** table including the two additions — each rests on a line I read, not on a name.

What remains unchecked: the auditor output was truncated for six of seven slices, so for every item I marked "†" in MISSED I can prove the control exists and is unverdicted in the text I was shown, but not that it is absent from their full tables — those should be reconciled against the complete slice files before acting. I did not exhaustively enumerate controls inside shared components for slices D1/D2/E2 (`account-settings.tsx`, `people-directory.tsx`, `user-lifecycle-dialog.tsx`, `organization-*`, `renewals-preview-panel.tsx`, `touchpoint-logger.tsx`, the `relationship-command-center` children), so slices C3, D2 and E2 are the least adversarially covered here — in particular I accepted IF-C3's job-sheet-editor findings and IF-E2's admin-route findings on their own evidence after confirming their invalidation and server-function claims, rather than re-deriving every control. I also did not verify capability-gating claims (`ROLE_GRANTS` coverage per button), n8n workflow inventory, or any screenshot/runtime behaviour; all of my verdicts are static-read verdicts.