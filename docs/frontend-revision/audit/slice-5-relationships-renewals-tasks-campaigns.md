## PARITY

| Route | Purpose (one clause) | Loader / routeQueryOptions | Query keys used | Server functions (read) | Mutations -> server function | Capabilities | Nav entry (yes/no + group) | Before: what actually works today | Integrity finding IDs |
|---|---|---|---|---|---|---|---|---|---|
| `/relationships` (`src/routes/relationships.tsx`) | Triage open relationship signals across accounts and dismiss them with a reason | **Cache-bypassing** — `loader: () => getRelationshipIndexRead({ data: initialRelationshipFilters })` (relationships.tsx:16); no `ensureQueryData`, no `routeQueryOptions`, no `loaderDeps`, no `validateSearch`. Component hand-writes `useQuery` with a duplicated `staleTime: 30_000` (relationships.tsx:28-34) | `crmQueryKeys.relationships.list({page,limit})` (line 29); `crmQueryKeys.relationships.lists()` for invalidation (line 45) | `getRelationshipIndexRead` (`src/server-functions/relationship-workspaces.ts:84`) | `dismissRelationshipSignalFn` (`src/server-functions/relationship-signals.ts:41`), called from `relationship-command-center.tsx:64-66`; page supplies only the invalidation callback (relationships.tsx:44-46) | read: `accounts.view` **and** `engagements.view` (`requireCapabilitySet`, relationship-workspaces.ts:88); write: `engagements.update` + `relationship_signal` ownership (relationship-signals.ts:44-47, Neon-resolved) | yes — "Retain" (`app-sidebar.tsx:60`, group label `renderGroup("Retain", retainItems)` line 147) | Signal list renders; Dismiss with a required reason reaches the server, optimistically removes the row and refetches the list (the `lists()` prefix does match the component's active `useQuery`, so this is **not** the VF-3 no-op case); pager buttons change the query key and refetch; an errored refetch shows a `role="alert"` banner | IF-D2-01, 02, 03, 04, 05, 25, 26 |
| `/renewals` (`src/routes/renewals.tsx`) | Renewal board grouped into five renewal-window columns with a per-engagement action panel | `context.queryClient.ensureQueryData(routeQueryOptions({ queryKey: crmQueryKeys.renewals.list(deps), queryFn: () => getRenewalsRead({ data: deps }) }))` (renewals.tsx:72-78); `loaderDeps` hard-codes `limit: 50` (line 70) | `crmQueryKeys.renewals.list(deps)` (line 75) — **loader only, no `useQuery` in the file**; `crmQueryKeys.renewals.lists()` + `engagements.*` + `clients.section(...)` invalidated from `renewals-preview-panel.tsx:24-51` | `getRenewalsRead` (`src/server-functions/operations.ts:106`); panel-lazy: `getEngagementsByClient`, `getClientContacts` (renewals-preview-panel.tsx:279-282) | `triggerRiskScoreAgent` (`engagements.ts:84`) via panel line 89; `createTouchpoint` (`touchpoints`) via `touchpoint-logger.tsx:86`; `renewEngagement` / `endEngagement` (`engagements.ts:62,73`) via `mark-renewed-ended-dialog.tsx:34,37`; `tidyTouchpointNote` (touchpoint-logger.tsx:76) | read: `engagements.view` + `products.view` (operations.ts:109-112); writes: `engagements.update` + engagement ownership; `agents.run` + engagement ownership (engagements.ts:87) | yes — "Retain" (`app-sidebar.tsx:61`) | All three `<Select>` filters and both pagers write search → loader re-runs server-side; card click opens the sheet; all four panel writes reach real server functions and the `missing_webhook` sentinel **is** branched on (panel:92-95); but nothing on the page re-reads after a write | IF-D2-06, 07, 08, 09, 10, 25, 26 |
| `/tasks` (`src/routes/tasks.tsx`) | Three-column task kanban with drag/keyboard status moves and task creation | `ensureQueryData(routeQueryOptions({ queryKey: crmQueryKeys.tasks.list(deps), queryFn: () => getTasks(...) }))` (tasks.tsx:57-63); component re-declares the same options via `useQuery({...routeQueryOptions({queryKey: tasksQueryKey ...}), initialData: loaderTasks})` (tasks.tsx:99-105) — same normalized key, so cache-backed | `crmQueryKeys.tasks.list(deps)` (60), `crmQueryKeys.tasks.list(filters)` (98), `crmQueryKeys.tasks.lists()` (135,136,143,158,178), `crmQueryKeys.tasks.detail(id)` (155) | `getTasks` (`src/server-functions/tasks.ts:39`) | `updateTask` (`tasks.ts:57`) via `move()` tasks.tsx:141 — optimistic `setQueriesData` + rollback + failure toast; `createTask` (`tasks.ts:49`) via tasks.tsx:173 | read `tasks.view`; create `tasks.create`; update `tasks.update` + `task` ownership (tasks.ts:42,52,60) | yes — "Retain" (`app-sidebar.tsx:62`) | The **best-wired route in the slice**: drag-drop and ←/→ both funnel into `move()`, which cancels, optimistically writes, calls `updateTask`, rolls back on failure with a toast, guards re-entry with `pendingTaskIds`, then invalidates both `tasks.detail(id)` and `tasks.lists()` with its own catch-toast; filters write to search and re-run loader + query | IF-D2-11, 12, 13, 14, 25, 26 |
| `/campaigns` (`src/routes/campaigns.tsx`) | Campaign/event index with create dialog; layout route rendering `<Outlet/>` unless exact path | `loader` returns a wrapper object: `{ campaignPage: await context.queryClient.ensureQueryData(routeQueryOptions({ queryKey: crmQueryKeys.campaigns.list(search), queryFn: () => getCampaignsPage({ data: search }) })) }` (campaigns.tsx:47-54) | `crmQueryKeys.campaigns.list(search)` (line 50) — loader only; **no `useQueryClient` is instantiated in this file at all** | `getCampaignsPage` (`src/server-functions/campaigns.ts:27`) | `createCampaign` (`campaigns.ts:42`) via campaigns.tsx:78 → `toast.success` → `navigate({to:"/campaigns/$id"})`; **no invalidation of any kind** | read `campaigns.view` (campaigns.ts:30); create `campaigns.manage` (campaigns.ts:45) | yes — "Convert" (`app-sidebar.tsx:54`) | Cards link to detail; `ListPagination` writes `page` to search → loader re-runs; the New-campaign dialog validates the name, disables while saving, creates a real campaign and navigates to it | IF-D2-15, 16, 17, 18, 19, 25, 26 |
| `/campaigns/$id` (`src/routes/campaigns.$id.tsx`) | Campaign follow-up workspace: attendee table, CSV attendee import, follow-up task generation | **Cache-bypassing** — `loader: ({ params }) => getCampaignWorkspaceRead({ data: { id: params.id } })` (campaigns.$id.tsx:37). Two hand-written `useQuery`s (73-78, 83-91) with duplicated `staleTime: 30_000`; no `routeQueryOptions`; no `validateSearch` | `crmQueryKeys.campaigns.detail(campaignId)` (74), `crmQueryKeys.campaigns.section(id,"attendees",{page,limit})` (84); invalidation table at 44-56 uses the filterless `section(id,"attendees")` plus `accounts.lists()`, `contacts.lists()`, `tasks.lists()` | `getCampaignWorkspaceRead`, `getCampaignWorkspaceSection` (`relationship-workspaces.ts:65,75`) | `validateEventImportRowsFn` (`event-import.ts:32`) via line 121; `commitEventImportFn` (`event-import.ts:45`) via line 152; `createCampaignFollowUpTasksFn` (`campaigns.ts:69`) via line 187 | read `campaigns.view` + `campaign` ownership; validate: `engagements.view`+`accounts.view`+`contacts.view`; commit: `engagements.create`+`campaigns.manage`+`accounts.create`+`contacts.create`; tasks: `tasks.create` + `campaign` ownership | no (reached from `/campaigns` cards and the back link) | CSV parse → server validate → per-row error list → commit; all three writes are real, all three have `isValidating`/`isImporting`/`isCreatingTasks` in-progress guards and try/catch with failure toasts; attendee pager and Retry both refetch; because the component holds a `useQuery` on `campaigns.detail(id)`, the mutation invalidations **do** refresh the visible page | IF-D2-20, 21, 22, 23, 24, 25 |

## INTEGRITY

| ID | Route | Element (file:line) | Category | Verdict | Owner | Fix step | Note |
|---|---|---|---|---|---|---|---|
| IF-D2-01 | /relationships | `useQuery({ queryKey: crmQueryKeys.relationships.list(...), ..., staleTime: 30_000 })` — `src/routes/relationships.tsx:28-34` | duplicate/inline query key (adjacent form: duplicated staleTime literal) | REAL | frontend | Replace the hand-written options with `...routeQueryOptions({ queryKey, queryFn })` so `CRM_STALE_TIME_MS` is the single source | The key itself comes from `crmQueryKeys`, so VF-8 holds; what is duplicated is the policy. `CRM_STALE_TIME_MS = 30_000` (`src/lib/performance/query-policy.ts:3`) — identical value, independently written. This is the only route in the slice that imports neither `routeQueryOptions` nor `CRM_STALE_TIME_MS`. |
| IF-D2-02 | /relationships | `loader: () => getRelationshipIndexRead({ data: initialRelationshipFilters })` — `src/routes/relationships.tsx:16`; fallback `relationshipData = relationshipQuery.data ?? initialRead` line 35; `refreshAfterDismiss` lines 44-46 | stale invalidation | REAL | frontend | Route the loader through `context.queryClient.ensureQueryData(routeQueryOptions({ queryKey: crmQueryKeys.relationships.list(initialRelationshipFilters), ... }))`, or add `router.invalidate({ filter: (m) => m.routeId === "/relationships" })` alongside the existing `invalidateQueries` | **Correction to the VF-3 expectation for this route:** the invalidation is *not* a no-op. `crmQueryKeys.relationships.lists()` = `["relationships","list"]` prefix-matches the active `useQuery` key `["relationships","list",{limit:50,page:n}]`, so the visible list does refetch after a dismiss. The residual defect is narrower: the loader snapshot `initialRead` is never refreshed, and it is the render fallback at line 35 and the `initialData` seed at line 31 — so a stale, already-dismissed signal can reappear whenever the query has no data. The loader also re-hits Postgres on every entry to `/relationships` even when the identical cache entry is fresh. |
| IF-D2-03 | /relationships | `const [relationshipPage, setRelationshipPage] = useState(1)` — `relationships.tsx:26`; pagers lines 77, 86 | non-URL pagination state (§16-adjacent: unscoped navigation) | REAL | frontend | Add `validateSearch` with `page`/`limit` and drive the query from `Route.useSearch()`, matching `/renewals` and `/campaigns` | The buttons do reach the server (the key changes, the query refetches), so the control is REAL. But the page number is invisible to the URL: a refresh, a shared link, or the browser Back button all silently return the user to page 1 while the loader also only ever fetches page 1 (line 13, `initialRelationshipFilters`). `/renewals` and `/campaigns` in the same slice both do this correctly. |
| IF-D2-04 | /relationships | "Dismiss" button `relationship-signal-card.tsx:56-64`; "Confirm dismiss" `relationship-signal-card.tsx:81`; handler `relationship-command-center.tsx:49-82` | disabled action without explanation (inverted: enabled action with no capability signal) | REAL | frontend + backend | Return the caller's `engagements.update` decision from `getRelationshipIndexRead` (the read model already loads the authorization context) and disable the button with a reason when it is false | **Direct answer to the slice question:** yes, dismiss reaches a server function — `dismissRelationshipSignalFn` (`src/server-functions/relationship-signals.ts:41`) — and yes it is capability-gated, at `requireCapability("engagements.update", { resourceType: "relationship_signal", resourceId })` (lines 44-47), whose ownership query resolves through **Neon**, not Supabase (`src/server/auth/resource-ownership.ts:56-57`). The gap is client-side only: the page loads on `accounts.view` + `engagements.view` (`relationship-workspaces.ts:88`), which is a strictly weaker set, so a view-only user sees a fully enabled Dismiss button and gets the generic `toast.error("Could not dismiss signal")` (command-center line 78) with no indication that it was a permissions refusal. There is no "resolve" control on this route — dismiss is the only signal action. |
| IF-D2-05 | /relationships | error banner `relationships.tsx:55-59` vs page caption line 68 | stale invalidation (misleading fallback state) | REAL | frontend | Either render the caption from the data actually on screen, or suppress the page counter while `isError` and no page-matched data exists | The banner reads "Showing the previous results", but the fallback chain at line 35 resolves to `initialRead`, which is **always page 1** (line 13 pins `page: 1`). On a failed fetch of page 3 the user sees page-1 rows under the caption "Page 3 of N" (line 68) with a banner asserting they are the previous results. |
| IF-D2-06 | /renewals | `invalidateRenewalMutation` — `src/components/renewals/renewals-preview-panel.tsx:53-64`, called at lines 99, 172-178, 222-227; consumed by a page that renders from `Route.useLoaderData()` at `src/routes/renewals.tsx:92` | stale invalidation | REAL | frontend | Add `router.invalidate({ filter: (m) => m.routeId === "/renewals" })` after each write, or give the page a `useQuery` on `crmQueryKeys.renewals.list(deps)` seeded with `initialData: loaderTasks`-style loader data, as `/tasks` does | **Largest defect in the slice.** `renewals.tsx` contains zero `useQuery`, zero `useRouter` and zero `router.invalidate` (verified by grep across the route and both `renewals/` components). Every row on the board comes from `renewalRead.rows` (line 97), a router loader snapshot. `invalidateQueries({ queryKey: crmQueryKeys.renewals.lists() })` marks the React Query entry stale but cannot push data into `useLoaderData`. Consequence: "Mark renewed", "Mark ended", "Log touchpoint" and "Re-score risk" all fire a success toast and the card stays in its original column with its original risk badge and renewal date until the user navigates away and back. |
| IF-D2-07 | /renewals | `limit: z.coerce.number()...default(50)` — `renewals.tsx:60`; `loaderDeps` hard-codes `limit: 50` — `renewals.tsx:70` | server capability with no UI path | UNAVAILABLE | frontend | Either add a page-size control that feeds `loaderDeps`, or drop `limit` from `renewalSearchSchema` so the URL contract stops advertising a knob that does nothing | `parseRenewalsInput` accepts `limit` (`src/server-functions/operations.ts:60`) and `normalizePagination` honours it, so the server path exists. The route deliberately severs it: the search param is parsed, then discarded before the loader sees it. No control anywhere in the file sets it. |
| IF-D2-08 | /renewals | `const today = new Date().toISOString().slice(0, 10)` — `renewals.tsx:96`, consumed by `getRenewalWindow(engagement.renewal_date, today)` line 114 | SSR-unsafe date (repo convention violation) | REAL | frontend | Use `getBusinessDateKey()` from `@/lib/business-date` as `/tasks` does (`tasks.tsx:97`), or a `format.ts` helper | `CLAUDE.md` states dates must go through `src/lib/format.ts` (fixed `en-GB` + UTC) to avoid SSR hydration mismatch. This call computes "today" during render on both server and client; across a UTC-midnight boundary the two disagree and cards hydrate into a different column than they were server-rendered in. `/tasks`, the sibling board, already uses the safe helper. |
| IF-D2-09 | /renewals | `AlertDialogAction onClick={confirm}` — `src/components/renewals/mark-renewed-ended-dialog.tsx:68`; handler lines 32-42 | missing failure feedback / missing in-progress state | REAL | frontend | Wrap `confirm` in try/catch with a `toast.error`, add a `saving` flag, and keep the dialog open on failure | `confirm` awaits `renewEngagement` or `endEngagement` with no try/catch and no pending state. Radix's `AlertDialogAction` closes the dialog on click, so on a rejection the dialog is already gone, `toast.success` never fires, `onDone` never runs, and the user is left looking at an unchanged board (see IF-D2-06) with no error at all. The only guard is `disabled={action === "end" && !reason.trim()}`, which covers validation, not failure. |
| IF-D2-10 | /renewals | "Save touchpoint" `src/components/touchpoint-logger.tsx:211`; handler `save` lines 85-100 | double-submission risk / missing in-progress state / missing failure feedback | REAL | frontend | Add a `saving` state, disable the button while the call is in flight, and try/catch with a failure toast | The button is never disabled and `save()` has no try/catch. Two clicks before the first `createTouchpoint` resolves create two touchpoint rows; a rejection produces an unhandled promise rejection with the dialog still open and no message. Note the "Tidy with AI" button in the same dialog (line 189-198) does this correctly — `tidying` flag plus catch-toast — so the pattern already exists one component away. |
| IF-D2-11 | /tasks | "Create" `src/routes/tasks.tsx:448`; `submit` lines 342-357; `onCreate` lines 172-180 | double-submission risk / missing in-progress state / missing failure feedback | REAL | frontend | Add a `saving` flag around `submit`, disable the button while in flight, and try/catch the `createTask` call with a `toast.error` | `onCreate` (172-180) awaits `createTask` with no try/catch; `submit` (342) awaits `onCreate` with no try/catch; the button carries no `disabled`. A double-click creates two tasks; a failure is an unhandled rejection with the dialog still open, no toast, and the optimistic `setQueryData` at line 174 never running. Compare `campaigns.tsx:352` which *does* carry `disabled={saving}` — the two dialogs in this slice disagree. |
| IF-D2-12 | /tasks | Assignee filter `<Select>` `tasks.tsx:213-225`; New-task Assignee `<Select>` `tasks.tsx:416-427`; owner render `tasks.tsx:298` | local-only select representing persisted state | REAL | frontend + backend | Replace `APP_USERS` with a server read of assignable profiles (the shell already loads `Profile`), and render owner names from that source | Both selects are populated from `APP_USERS` (`src/lib/users.ts:10-41`) — a five-entry hardcoded fixture with placeholder ids `00000000-0000-0000-0000-00000000000{1..5}` and names "Ada Wong", "Marcus Lee", "Priya Shah", "Kenji Tan", "Sara Lin". The filter *is* REAL by decision order (it reaches `getTasks` → `listTasks`), but it sends a fixture UUID as `assigned_to`, which will match no real Neon task, so picking any assignee empties the board. The create dialog writes that fixture UUID into `tasks.assigned_to`. And line 298 renders `userById(t.assigned_to)?.name`, which is `undefined` — blank — for every genuinely-assigned task. |
| IF-D2-13 | /tasks | `const filtered = useMemo(() => rows, [rows])` `tasks.tsx:117`; header string `` `${filtered.length} of ${rows.length} tasks` `` `tasks.tsx:169` | misleading count (dead client-side filter) | READ-ONLY | frontend | Delete the identity memo and render `${rows.length} tasks`, or make the header report the server total against an unfiltered count | `filtered` is `rows` by identity, so the two numbers are the same value by construction and the header can only ever read "N of N tasks" — it presents a filter relationship that does not exist. Filtering is entirely server-side via `getTaskReadInput` (tasks.tsx:47-50). |
| IF-D2-14 | /tasks | `tasksQuery` `tasks.tsx:99-105` — `isError` referenced nowhere in the file | missing failure feedback | REAL | frontend | Render an alert (or reuse the `role="alert"` pattern from `relationships.tsx:55-59`) when `tasksQuery.isError` and fall back to the last good data explicitly | Because `initialData: loaderTasks` is set, `data` is always defined and a failed background refetch is completely invisible: the board keeps showing pre-mutation task positions with no indication the refresh failed. The `move()` handler already toasts "Task saved, but the board could not refresh." (line 161) for the invalidation path, so the failure vocabulary exists — the query path just does not use it. |
| IF-D2-15 | /campaigns | `create` — `src/routes/campaigns.tsx:77-82` | stale invalidation | REAL | frontend | Instantiate `useQueryClient()` and `await queryClient.invalidateQueries({ queryKey: crmQueryKeys.campaigns.lists() })` before navigating | `createCampaign` succeeds, toasts, and navigates to the detail route. **No `queryClient` is instantiated anywhere in campaigns.tsx** — grep confirms no `useQueryClient`, no `router.invalidate`. The loader is cache-backed via `ensureQueryData` with `staleTime = CRM_STALE_TIME_MS` (30 s), so returning to `/campaigns` inside that window serves the pre-create cache entry and the new campaign is absent from its own index. The navigate-away masks it only until the user goes back. |
| IF-D2-16 | /campaigns | `create` `campaigns.tsx:77-82`; `submit` `campaigns.tsx:201-227` | missing failure feedback | REAL | frontend | Try/catch around `await onCreate(...)` in `submit` with a `toast.error`, leaving the dialog open | `submit` uses `try { ... } finally { setSaving(false) }` with **no catch**, and `create` has no try/catch either. A rejected `createCampaign` (e.g. a `campaigns.manage` denial) resolves to an unhandled rejection: `saving` flips back to false, the button re-reads "Create campaign", the fields are still populated, and the user is given no reason. The in-progress state (line 352 `disabled={saving}`) is correct — only the failure branch is missing. |
| IF-D2-17 | /campaigns | Owner `<Select>` `campaigns.tsx:313-324`; value passed at line 213 | local-only select representing persisted state | READ-ONLY | frontend + backend | Either remove the Owner select from the create dialog, or have `createCampaign` honour an explicitly supplied `owner` after an authorization check | The chosen owner never reaches the database: `createCampaign` spreads the payload and then **overrides** it — `createCampaignInNeon({ ...data, owner: session.profile.id })` (`src/server-functions/campaigns.ts:47`). The campaign is always owned by whoever created it. `CreateCampaignInput` does declare `owner` as assignable (`src/server/repositories/campaigns.ts:14-17`), and campaign ownership is load-bearing for authorization (`resource-ownership.ts:40`), so this silently mis-assigns access scope. The option list is the same `APP_USERS` fixture as IF-D2-12. |
| IF-D2-18 | /campaigns | `status`, `type`, `owner` in `campaignListSearchSchema` — `campaigns.tsx:39-41` | server filter with no UI path | UNAVAILABLE | frontend | Add three `<Select>` filters writing to search, mirroring `/renewals` lines 150-197 | These three params are validated, passed straight into `getCampaignsPage({ data: search })` (line 51), and genuinely honoured server-side — `listCampaignsPage` builds `buildFilters([["status",...],["type",...],["owner",...]])` (`src/server/repositories/campaigns.ts:60-66`). No control anywhere in the file sets them, so the filtering capability is reachable only by hand-editing the URL. |
| IF-D2-19 | /campaigns | `SummaryCard` tiles `campaigns.tsx:111-113`, component `campaigns.tsx:163-173` | misleading metric | READ-ONLY | frontend | Compute the tiles from server aggregates, or label them "on this page"; `campaignPage.total` is already in scope at line 104 | All three tiles derive from `campaigns = campaignPage.items` (line 71), i.e. the current 50-row page only — `campaigns.length`, then `.filter(status === "active")` and `.filter(status === "completed")` (lines 74-75). The tile labelled "Campaigns" therefore reads 50 no matter how many exist, while `campaignPage.total` sits unused two lines away. Also page-local rather than the shared `MetricStrip`. |
| IF-D2-20 | /campaigns/$id | `loader: ({ params }) => getCampaignWorkspaceRead(...)` `campaigns.$id.tsx:37`; invalidation table `campaigns.$id.tsx:44-56` | stale invalidation | REAL | frontend | Route the loader through `ensureQueryData(routeQueryOptions({ queryKey: crmQueryKeys.campaigns.detail(params.id), ... }))`, and add `crmQueryKeys.campaigns.lists()` to the `attendee_import` and `follow_up_tasks` key sets | Two-part. (a) The loader bypasses the cache (VF-3), but unlike the true no-op cases the page holds a `useQuery` on the *same* key `crmQueryKeys.campaigns.detail(campaignId)` (line 74), so `invalidateCampaignMutation` **does** refresh what is on screen — the stale artefact is only the `Route.useLoaderData()` snapshot at line 70, used as `initialData`. (b) The real leak: neither mutation invalidates `crmQueryKeys.campaigns.lists()`, so after importing 300 attendees or generating follow-up tasks, `/campaigns` still serves its cached page. Combined with IF-D2-15 the campaigns index is never invalidated by anything. |
| IF-D2-21 | /campaigns/$id | `importRows` `campaigns.$id.tsx:144-177` → `commitEventImportFn` (`src/server-functions/event-import.ts:45`) → `commitEventImport` (`src/server/repositories/event-import.ts:84-154`) | replay/retry without idempotency | REAL | backend | Give `campaign_members` a dedupe key (campaign_id + normalized email/contact) with `on conflict do nothing`, and pass `campaignId` into `validateEventImportRowsFn` so already-imported rows surface as a validation warning before commit | `commitEventImport` loops the rows and calls `createCampaignMember(...)` unconditionally for every one (line 137-152) — no existing-member lookup, no unique constraint referenced, no `on conflict`. `validateEventImportRowsFn` (`event-import.ts:32-43`) is never given the campaign id at all, so it structurally *cannot* detect a re-import; it only loads account candidates and their contacts. Re-uploading the same CSV silently doubles the attendee roster and the `attendeeSummary.total` tile. The UI guards are in-flight only (`isImporting`, line 145) and do not survive a second file selection. Contrast `createCampaignFollowUpTasks` (`src/server/repositories/campaign-follow-ups.ts:55-104`), which *is* idempotent — `where follow_up_status = 'not_started' ... for update` plus a conditional update — so the neighbouring action already demonstrates the right pattern. |
| IF-D2-22 | /campaigns/$id | `workspaceQuery` `campaigns.$id.tsx:73-78` — `isError` referenced nowhere | missing failure feedback | REAL | frontend | Surface `workspaceQuery.isError` next to the header, as the attendee card already does at lines 243-249 | With `initialData: initialRead` set, `workspaceQuery.data` is always defined, so a failed refetch of campaign name, status, dates and `attendeeSummary` is completely silent — including immediately after an import, where the summary counts are exactly what the user is checking. The attendee query five lines below handles its own error correctly with an alert and a Retry, so the page is internally inconsistent. |
| IF-D2-23 | /campaigns/$id | `const [attendeePage, setAttendeePage] = useState(1)` `campaigns.$id.tsx:81`; `onPageChange={setAttendeePage}` line 258 | non-URL pagination state | REAL | frontend | Add `validateSearch` with an `attendeePage` param and drive `attendeeFilters` from `Route.useSearch()` | The pager does reach the server — it changes `crmQueryKeys.campaigns.section(id,"attendees",{page,limit})` and the query refetches — but the page number is invisible to the URL, so refresh/share/Back all reset to page 1. Same class as IF-D2-03. |
| IF-D2-24 | /campaigns/$id | route options `campaigns.$id.tsx:36-42` — no `errorComponent`, no `notFoundComponent` | raw database or driver error shown to users | REAL | frontend | Add a route `errorComponent` (and a `notFoundComponent`, as `clients.$id.tsx` already has) that runs the message through a sanitizer before display | `campaigns.id` is `uuid primary key` (`neon/migrations/003_client_relationship_360.sql:48`), so `/campaigns/<not-a-uuid>` reaches Postgres and raises `22P02 invalid input syntax for type uuid: "…"`. With no route-level boundary this propagates to the root handler, which renders `{error.message}` verbatim (`src/routes/__root.tsx:60`). The well-formed-but-missing case is handled more gracefully — `getCampaignWithAttendeeSummary` throws `"Campaign not found"` (`src/server/repositories/campaigns.ts:169`) — which is exactly why the malformed case is the one worth fixing. |
| IF-D2-25 | all five (`/relationships`, `/renewals`, `/tasks`, `/campaigns`, `/campaigns/$id`) | none of the five declares `errorComponent`; the inherited handler renders `<p …>{error.message}</p>` at `src/routes/__root.tsx:60` | raw database or driver error shown to users | REAL | frontend | Land `src/lib/errors.ts` `toSafeErrorMessage` (repo-map §Libraries, B5) and apply it in `__root.tsx:60`, then add route-level boundaries where a per-route message helps | Every loader failure on these five routes — an authorization denial from `requireCapabilitySet`/`requireCapability`, a `42P01`/`22P02` driver error, or the Supabase 500 described in VF-7 — is rendered to the user as the raw exception string on a full-page takeover. `src/server/db/postgres-error.ts` already classifies driver failures server-side but nothing consumes it for UI messaging. |
| IF-D2-26 | /relationships, /renewals, /tasks, /campaigns | `app-sidebar.tsx:54` (Campaigns), `:60` (Relationships), `:61` (Renewals), `:62` (Tasks); rendered unconditionally at `app-sidebar.tsx:146-147` | unscoped or capability-inappropriate navigation | REAL | frontend + backend | Resolve per-route capabilities in the shell read (as `adminNavigation` already is) and hide or disable-with-reason the entries the caller cannot open | Only the Administration group is capability-resolved (`app-sidebar.tsx:149-160`, with first-destination resolution). Convert and Retain render every item to every signed-in user, while each loader hard-requires capabilities and **throws** rather than degrading: `/relationships` needs *both* `accounts.view` and `engagements.view` (`requireCapabilitySet`, `relationship-workspaces.ts:88` — `requireCapabilitySet` throws on the first missing required capability, `authorization.server.ts:180-184`); `/renewals` needs `engagements.view` **and** `products.view` (`operations.ts:109-112`); `/tasks` needs `tasks.view` (`tasks.ts:42`); `/campaigns` needs `campaigns.view` (`campaigns.ts:30`). The click therefore lands on the raw error page of IF-D2-25. |

## REVIEWED-OK

Controls traced to an authorized server function or to legitimate local state, with no defect found.

**/relationships**
- Previous / Next pager `relationships.tsx:72-89` — REAL. Correctly `disabled` on boundary *and* on `relationshipQuery.isFetching`, `aria-label`ed, and each click changes `crmQueryKeys.relationships.list({page,limit})` so `getRelationshipIndexRead` runs. (URL-state gap tracked separately as IF-D2-03.)
- Dismiss reason `<Input onChange>` `relationship-signal-card.tsx:72-78` — local form state, correctly `disabled={isDismissing}`.
- "Cancel" `relationship-signal-card.tsx:84-91` + `cancelDismiss` `relationship-command-center.tsx:41-47` — local state, correctly refuses to close while a dismiss is pending.
- `dismiss()` re-entry guard `relationship-command-center.tsx:50-52` + `pendingSignalIds` — genuine double-submission protection; empty-reason validation at lines 56-59 matches the server's own `parseDismissRelationshipSignalInput` requirement (`relationship-signals.ts:23-25`). Note: `dismissRelationshipSignal` (`src/server/repositories/relationship-signals.ts:224-235`) has no `and dismissed_at is null` guard, so a replayed dismiss would overwrite the original reason/actor — unreachable through this UI because the row is removed and the pending guard holds, so not filed.
- Empty state `relationship-command-center.tsx:84-91` and the "Showing the 10 highest-priority signals per account" note at line 97 — the hardcoded 10 correctly matches the SQL slice `[1:10]` at `src/server/repositories/relationship-signals.ts:123`.
- Error banner presence `relationships.tsx:55-59` — this route is the only one in the slice that surfaces a query error at all (the wording is what IF-D2-05 addresses, not the existence).

**/renewals**
- Risk `<Select>` `renewals.tsx:150-163`, Product `<Select>` `renewals.tsx:164-179`, Renewal-window `<Select>` `renewals.tsx:180-197` — all REAL. `setFilters` (line 100) writes to search and resets `page: 1`; `loaderDeps` picks the three up (lines 66-69) so the loader re-runs and `parseRenewalsInput` (`operations.ts:42-62`) applies them server-side. All three carry `aria-label`s. Product options come from server data (`renewalRead.products`), not a fixture.
- Previous / Next renewal page `renewals.tsx:202-221` — REAL, boundary-disabled against `lastPage`, writes `page` to search.
- Empty-state actions `renewals.tsx:232-238` — real `<Link>`s to `/clients` and `/`, both authenticated routes that exist.
- `RenewalCard` click / Enter / Space `renewal-card.tsx:28-41`, and the close button `renewals-preview-panel.tsx:117` — READ-ONLY: they open and close a panel over already-loaded loader data and change nothing persisted. Correct as-is.
- "Re-score risk" `renewals-preview-panel.tsx:180-191` → `triggerRiskScoreAgent` (`engagements.ts:84`, gated `agents.run` + engagement ownership) — REAL, with in-progress state (`scoreStatus`), a skeleton (lines 135-140), a failure branch (line 161), an `already_running` branch (line 90), **and a correct `missing_webhook` branch** (lines 92-95) naming `N8N_SCORE_RENEWAL_RISK_WEBHOOK_URL`. This is one of the five call sites that handle the sentinel properly — unlike `accounts.$id.tsx` / `leads.$id.tsx`.
- "Draft renewal quote" `renewals-preview-panel.tsx:192-203` — REAL navigation to `/quotes/new` with `clientId`/`productId` search params; `/quotes/new` is a registered route that accepts them.
- "Log touchpoint" trigger `renewals-preview-panel.tsx:271-292` — REAL: lazily calls `getEngagementsByClient` + `getClientContacts` in parallel on first open, memoized per client, with a `loadingByClientId` disable guard and a `finally`. (The *save* inside the dialog is IF-D2-10.)
- "Tidy with AI" `touchpoint-logger.tsx:189-198` → `tidyTouchpointNote` — REAL, correctly hidden behind `isAiNoteTidyAvailable()` (line 69), disabled while tidying or when notes are empty, and catch-toasted.
- Touchpoint Type / Sentiment / Engagement / Contact selects `touchpoint-logger.tsx:114,131,151,169` — local form state feeding `createTouchpoint`; engagement and contact option lists come from server reads, not a fixture.
- `MarkRenewedEndedDialog` reason validation `mark-renewed-ended-dialog.tsx:68` (`disabled` when ending without a reason) — correct and matches `endEngagement`'s required `reason`.
- Type note, not filed as a finding: `Route.useLoaderData() as unknown as RenewalsView` (`renewals.tsx:92`) erases the loader's real return type. The shapes do line up — `loadRenewalsRead` returns `{rows,total,page,limit,metrics,products,asOf}` (`src/server/read-models/operations.ts:42-53`) — so nothing is broken today, but the double cast means a server-side shape change would fail at runtime instead of at `tsc`.

**/tasks**
- Priority `<Select>` `tasks.tsx:197-212` — REAL: writes to search, `loaderDeps` picks it up, `getTaskReadInput` maps `"all" → undefined`, and `getTasks` filters on it (`tasks.ts:44-46`). `aria-label`ed.
- Drag-and-drop `onDragStart`/`onDragOver`/`onDrop` `tasks.tsx:239-243, 265-268` and keyboard ←/→ `tasks.tsx:269-276` — both REAL, both funnelling into the same `move()` and therefore into `updateTask` (`tasks.ts:57`, gated `tasks.update` + `task` ownership).
- `move()` itself `tasks.tsx:129-163` — the reference implementation for this repository's imperative-write pattern: re-entry guard (130), no-op guard (132), `cancelQueries` (135), optimistic `setQueriesData` (136), rollback + `toast.error("Task move failed. Try again.")` on rejection (143-148), then `tasks.detail(id)` (exact) + `tasks.lists()` invalidation wrapped in its own catch-toast (152-162). Nothing to fix.
- Per-card accessibility `tasks.tsx:259-281` — `role="button"`, `tabIndex` flipped to `-1` while pending, `aria-busy`, `aria-disabled`, `draggable={!isPending}`, descriptive `aria-label` naming the keyboard affordance, and a `cursor-wait opacity-60` in-progress affordance. Exemplary.
- New-task Title / Description / Due inputs `tasks.tsx:375-382, 388-394, 433-440` and Priority select `tasks.tsx:401-410` — local form state; all labelled with `htmlFor`/`id` pairs.
- Empty-column `WorkSurfaceEmpty` `tasks.tsx:308-313` — shared component, correct per column.
- "Cancel" `tasks.tsx:445-447` — local dialog close.
- Title-required validation `tasks.tsx:343-346` — correct client guard before the server call.

**/campaigns**
- `ListPagination onPageChange` `campaigns.tsx:102-109` → shared `list-pagination.tsx:12-48` — REAL: writes `page` to search, loader re-runs. The shared component is boundary-disabled, `aria-label`ed on both buttons, wrapped in `<nav aria-label="List pagination">`, and its range readout is `aria-live="polite"` with `tabular-nums`.
- Campaign card `<Link to="/campaigns/$id" params={{id}}>` `campaigns.tsx:123-128` — REAL router link (not a raw anchor), with a visible focus ring.
- "New campaign" trigger `campaigns.tsx:92-95` and `Dialog onOpenChange` line 90 — local open state.
- Name / Start date / End date / Objective / Notes fields `campaigns.tsx:239-246, 287-294, 300-307, 330-336, 342-348` — local form state, all `htmlFor`/`id` labelled, all persisted by `createCampaign` (`CreateCampaignInput` accepts every one — `src/server/repositories/campaigns.ts:14-17`).
- Type `<Select>` `campaigns.tsx:252-264` and Status `<Select>` `campaigns.tsx:270-281` — READ-ONLY at the control level but their values *do* persist through `createCampaign`, and both option sets exactly match the DB check constraints (`neon/migrations/003_client_relationship_360.sql:50-53`). Unlike Owner (IF-D2-17), these are honoured.
- Name-required validation `campaigns.tsx:202-206` and `disabled={saving}` `campaigns.tsx:352` — correct client guard and correct in-progress state.

**/campaigns/$id**
- "Campaigns" back button `campaigns.$id.tsx:205-210` — REAL `<Link>` via `asChild`.
- "Choose CSV" proxy button `campaigns.$id.tsx:284-291` — local `fileInputRef.current?.click()`; the hidden `<input type="file" onChange>` at lines 292-305 is the control that matters and it does reach `validateEventImportRowsFn`. Both correctly `disabled={isValidating}`; the input is `sr-only` with a real `id`.
- `onFile` `campaigns.$id.tsx:108-142` — REAL: parses locally, validates server-side, sets a per-row error list, resets the input on every failure path, and has `isValidating` + try/catch/finally. Zero-row and parse-failure branches both toast.
- "Import N attendee rows" `campaigns.$id.tsx:345-353` → `commitEventImportFn` — REAL, gated on four capabilities server-side, `disabled` on empty rows / outstanding errors / in-flight import / in-flight validation, with a re-entry guard at line 145, a `{ok:false}` validation-failure branch (154-161), and a catch-toast. (Its idempotency gap is IF-D2-21, a backend concern.)
- "Create follow-up tasks" `event-attendee-table.tsx:80-82` → `createFollowUpTasks` `campaigns.$id.tsx:179-197` → `createCampaignFollowUpTasksFn` (`campaigns.ts:69`, gated `tasks.create` + `campaign` ownership) — REAL, `isCreatingTasks` guard, try/catch/finally, reports the actual `createdTasks` count. **Idempotent server-side**: `campaign-follow-ups.ts:56-102` selects only `follow_up_status = 'not_started'` `for update` and updates conditionally, so a second click creates zero tasks rather than duplicates.
- Attendee "Retry" `campaigns.$id.tsx:246-248` — REAL, calls `attendeeQuery.refetch()`.
- Attendee pager `event-attendee-table.tsx:140-157` — REAL, boundary-disabled, `aria-label`ed. (URL-state gap is IF-D2-23.)
- Validation-error list `campaigns.$id.tsx:329-343` — shows row numbers and reasons, caps at 6 with a "+N more", and the reasons come from the app's own validator, not from the driver.
- Empty attendee state `event-attendee-table.tsx:65-72` — the "Create follow-up tasks" button disappears with it, which is correct: with no attendees there is nothing to follow up.
- Authorization note across the slice: every resource type these routes target — `relationship_signal`, `campaign`, `engagement`, `task` — resolves through **Neon**, not Supabase (`src/server/auth/resource-ownership.ts:35-58`). None of these five routes is exposed to the VF-7 `SUPABASE_URL`-missing 500.

## PATTERNS

| Route | Header | Metrics | Filters | Table or list impl | Empty state | Loading state | Error state | Status badge | Detail panel | Action placement |
|---|---|---|---|---|---|---|---|---|---|---|
| `/relationships` | shared component `PageHeader` (`relationships.tsx:50`) | **missing** — no metric row at all, though `total` and `hiddenSignalCount` are computed at lines 38-42 | **missing** — no severity or signal-type filter, despite `parseRelationshipIndexInput` accepting `severity` and `signalType` (`relationship-workspaces.ts:38-50) | shared component `RelationshipCommandCenter` → `RelationshipSignalCard` (card list, not a table) | shared component — `relationship-command-center.tsx:84-91`, but component-local, **not** `WorkSurfaceEmpty` | **missing** — `isFetching` only disables the pagers (lines 76, 85); no skeleton, no pending UI | page-local — `role="alert"` banner `relationships.tsx:55-59` (the only route in the slice with one; wording defect = IF-D2-05) | shared component `StatusBadge` for severity (`relationship-signal-card.tsx:47`) | **missing** — dismissal is an inline expander on the card (`relationship-signal-card.tsx:67-95`), not `SalesContextPanel` | inline on each card; `PageHeader` receives no `actions` |
| `/renewals` | shared component `CommandHeader` (`renewals.tsx:124-128`) | shared component `MetricStrip` (`renewals.tsx:131-146`), exactly 4 metrics, `tabular-nums` via `MetricCard` (`metric-card.tsx:25`) | page-local — three `<Select>`s in a bare `<Card className="p-3">` (`renewals.tsx:148-224`), all URL-backed | page-local — five-column grid of shared `RenewalCard`s (`renewals.tsx:242-269`), no `<Table>` | shared component `WorkSurfaceEmpty` (`renewals.tsx:227-240`) **plus** a page-local per-column dashed placeholder (`renewals.tsx:252-255`) — two empty-state vocabularies on one page | **missing** — no `pendingComponent`, no skeleton | **missing** — inherits `__root.tsx` `ErrorComponent` (IF-D2-25) | shared component `StatusBadge` for `renewal_risk` (`renewal-card.tsx:46`) | page-local `RenewalsPreviewPanel` (`src/components/renewals/renewals-preview-panel.tsx`) on a shadcn `Sheet` — **not** the barrel's `SalesContextPanel` | filters + pager in a filter card; write actions in the Sheet's 2-col button grid (`renewals-preview-panel.tsx:167-210`); `CommandHeader` receives no `actions` |
| `/tasks` | shared component `CommandHeader` (`tasks.tsx:166-183`) | shared component `MetricStrip` (`tasks.tsx:186-193`), exactly 4 metrics | page-local — two `<Select>`s in a bare `<Card className="p-3">` (`tasks.tsx:195-230`), both URL-backed; assignee options are a code fixture (IF-D2-12) | page-local — three-column kanban of `<Card>`s with native HTML5 drag events (`tasks.tsx:232-318`) | shared component `WorkSurfaceEmpty` per column (`tasks.tsx:308-313`) | **missing** — `initialData` means the query is never `isPending`; per-card in-progress state does exist (`aria-busy`, `cursor-wait`, `tasks.tsx:262-280`) | **missing** — `tasksQuery.isError` unreferenced (IF-D2-14); `move()` failure *is* toasted (line 146) | shared component `StatusBadge` for priority (`tasks.tsx:285`) | **missing** — no per-task detail surface | shared header slot — `CommandHeader actions={<NewTaskDialog/>}` (`tasks.tsx:170-182`); the only route in the slice using the header's action slot for a write |
| `/campaigns` | shared component `PageHeader` (`campaigns.tsx:86-100`) | page-local `SummaryCard` ×3 (`campaigns.tsx:110-114`, component at 163-173) — **not** `MetricStrip`, page-scoped counts (IF-D2-19), no `tabular-nums` | **missing** — schema accepts `status`/`type`/`owner`, no control exists (IF-D2-18) | page-local — card grid of `<Link>`-wrapped `<Card>`s (`campaigns.tsx:121-157`), no `<Table>` | page-local — bare dashed `<div>` (`campaigns.tsx:116-119`), **not** `WorkSurfaceEmpty` | **missing** | **missing** — inherits root (IF-D2-25) | shared component `StatusBadge` (`campaigns.tsx:138`); campaign *type* uses the duplicated `replace(/_/g," ")` fallback instead (`campaigns.tsx:148`) | **missing** — navigation to `/campaigns/$id` instead | shared header slot — `PageHeader actions={<Dialog>…}` (`campaigns.tsx:89-99`); pagination above the metric row (`campaigns.tsx:102`) rather than below the list |
| `/campaigns/$id` | shared component `PageHeader` (`campaigns.$id.tsx:201-212`) | page-local `SummaryCard` ×4 (`campaigns.$id.tsx:215-231`, component at 377-387) — a **second, differently-shaped** local `SummaryCard` from the one in `campaigns.tsx`; not `MetricStrip`, no `tabular-nums` | **missing** — attendee list has pagination only, no status/follow-up filter | shared component `EventAttendeeTable` (`src/components/relationship/event-attendee-table.tsx`) using shadcn `<Table>`, wrapped in `overflow-x-auto` | shared component — `event-attendee-table.tsx:65-72`, but component-local dashed `<div>`, **not** `WorkSurfaceEmpty` | page-local — `<p role="status">Loading attendees...</p>` (`campaigns.$id.tsx:239-242`); plus real in-progress button labels for all three writes | page-local — `role="alert"` + Retry for the attendee query (`campaigns.$id.tsx:243-249`); **missing** for `workspaceQuery` (IF-D2-22) and for the loader (IF-D2-24, IF-D2-25) | shared component `StatusBadge` ×4 (`campaigns.$id.tsx:216, 363`; `event-attendee-table.tsx:114, 122, 125`) — the conversion cell passes an explicit `label` with the same `replace(/_/g," ")` fallback (line 127) | n/a — this route *is* the detail | header slot holds only the back `<Link>` (`campaigns.$id.tsx:205-210`); the three real write actions live inside body cards (import button `:345`, follow-up button `event-attendee-table.tsx:80`) rather than in the header |